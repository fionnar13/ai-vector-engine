# PHASE 3.13 BUILD REPORT

Status: PASS

Measured on branch `feat/phase-3.13-ai-planner` @ 75e8630 (= origin/main), working tree uncommitted (spec §40.4-5). All numbers below were re-measured at Checkpoint H (2026-09-21); transcripts in `scripts/phase3.13-evidence/3.13-H-npm-test.txt` and `3.13-H-typecheck.txt`.

## Implemented

- **Checkpoint A — data types (29 tests, tests/ai.test.mjs:33-225)**
  - 12 planning error codes (11 spec-mandated + `TOOL_REGISTRY_INVALID` added at Checkpoint D): `src-js/ai.js:78` (`PlanningErrorCodes`), `PlanningError` ai.js:98.
  - `validateIntent` with strict per-category unknown-key rejection: ai.js:372 (validators ai.js:187-370); categories ai.js:170.
  - `buildExpectedState` mechanical derivation, status `'requested'` (§09): ai.js:393; `validateExpectedState` (plain-data/function-value scan): ai.js:441.
  - `makePlanStep`/`makePlan`/`validatePlanStep`/`validatePlanStructure` (§10/§18 structural subset): ai.js:499/506/518/532.
  - `createPlanningContext` — deep-frozen plain-data projection, function values rejected (§13): ai.js:566-596.
  - Determinism primitives (§16): `stableStringify` key-sorted ai.js:149, `fnv1a32` content-derived ids ai.js:159; zero entropy sources.
- **Checkpoint B — planner core (30 B-labeled tests, tests/ai.test.mjs:262-487)**
  - `createExpectedState` ai.js:864; `createPlan` ai.js:881 with create rules A–D (`buildCreatePlanSteps` ai.js:652: T01 geometry-only → T07 fill → T08 both/center); `resolveOriginXY` ai.js:631; `readArtboardCenter` ai.js:611 (§24: artboard never guessed).
  - `validatePlan` reference/ordering layer (`collectDollarRefs` ai.js:903, `checkStepReferences` ai.js:921) incl. the §20 Planner rule (centered ExpectedState ⇔ T08 both/center, mandated comment) asserted at tests/ai.test.mjs:383/391.
  - `compilePlanToDSL` — mechanical mapping onto the EXISTING DSL (§23/§31, no second DSL): ai.js:1340 (step compiler ai.js:1245).
  - §16 byte-identical determinism proven in tests :471-487 and standalone probe `scripts/probe-3.13-checkpointB-determinism.mjs` (hashes stable through G: `3.13-G-determinism-regression.txt`).
- **Checkpoint C — planning rules E/F/G/H (38 C-labeled tests, tests/ai.test.mjs:353 + :509-754)**
  - Rule E transform (`buildTransformPlanSteps` ai.js:773: T05 translate, T06 scale/rotate/matrix resolved at plan time mirroring dsl.js:420-428); Rule F appearance (`buildAppearancePlanSteps` ai.js:806: per-target T07 — DSL appearance op is single-target, dsl.js:114); Rule G alignment (`buildAlignmentPlanSteps` ai.js:822: one T08, axis/mode passthrough); Rule H structure (`buildStructurePlanSteps` ai.js:837: group T10 ≥2 targets, ungroup T11 per-target); dispatcher ai.js:855.
  - `$doc:<objectId>` plan-level reference kind + strict resolution (`objectsSnapshotOf` ai.js:724, `resolveDocRefs` ai.js:739, own-property reads); `checkDocReferences` ai.js:983 wired into `validatePlan` ai.js:1197.
- **Checkpoint D — tool registry integration (9 tests, tests/ai.test.mjs:819-942)**
  - Exactly one substrate import: `import { createCoreToolRegistry } from './tools.js'` — src-js/ai.js:55; frozen read-only projection `PLANNER_TOOL_REGISTRY {has,get,list}` ai.js:65; export `getPlannerRegistry` ai.js:74; execute/validate/register/unregister structurally absent from what Planner code can reach.
  - `validatePlan(plan, context, registry)` registry block: `requireRegistryFacade` ai.js:1022, `checkToolDefinition` ai.js:1061, `validateInputAgainstSchema` ai.js:1104, `validatePlanStepTools` ai.js:1153; codes TOOL_NOT_FOUND / TOOL_REGISTRY_INVALID (12th) / TOOL_INPUT_INVALID; mutation-only plan-steps rule (read T16-T18 / proposal T19-T20 not plannable).
  - createPlan post-condition: every emitted toolId must resolve in the live registry. Mutation tools planned but NEVER executed during planning (spy-facade tripwires, test D-6 :872); DI surface (optional registry param, determinism-neutral, test D-8 :895).
- **Checkpoint E — DSL/IR substrate extension (15 E-labeled tests, tests/ai.test.mjs:942-1019 + E-RT :1064-1079)**
  - `src-js/dsl.js` ONLY: `analyzeReferences(program, context)` gains the docObjects gate + own-property `isDocRef` + external-ref exemption; context threaded through `validateSemantics`/`validateDSL`/`compileToIR`/`compileDSL`; `mapToToolIR` threading-only (targets carried verbatim). Diff vs pristine: **+35/−11** (evidence `3.13-E-dsl-diff.txt`; pristine snapshot `dsl.js.checkpointD.pristine` byte-identical to origin/main — see Deviations H-1 for the +36/−12 transcription note).
  - `compilePlanToDSL` strips the `$doc:` prefix before the executor (ai.js:1237 `stripRefPrefix`); full round trip Plan → DSL → parseDSL → validateDSL(ctx) → compileToIR proven same-toolIds/inputs/targets for every rule A–H.
- **Checkpoint F — mandatory vertical slice (4 tests, tests/ai.test.mjs F-S1 :1253 / F-S2 :1266 / F-S3 :1280 / F-S4 :1292)**
  - §41-F four-step proof over the REAL pipeline (real stores + SceneGraph + EventBus + HistoryManager + TransactionExecutor + `createCoreToolRegistry` + DSLExecutor), zero substrate changes; harness composition-layer wiring only (SpatialIndex invalidation mirrors renderer.js:317-321 — harness subscription at tests/ai.test.mjs:1179).
- **Checkpoint G — architecture tests (6 tests, tests/ai.test.mjs G-1 :1421 / G-2 :1429 / G-3 :1435 / G-4 :1445 / G-5 :1453 / G-6 :1469)**
  - Comment/string-aware static source scan of src-js/ai.js (same-length placeholders, char/line 1:1), word-boundary patterns, import contract pinned to exactly `['./tools.js']`, zero npm dependencies proven at package.json + whole-src-js level, anti-vacuity guard. Probe-validated before embedding (`scripts/probe-3.13-checkpointG-scan.mjs`).

## Files

Delta vs origin/main (75e8630), `git diff --numstat` + `git status --porcelain`, measured at Checkpoint H:

| File | Status | Lines | sha256 |
|---|---|---|---|
| `src-js/ai.js` | NEW (untracked) | 1350 (`wc -l`) | `e3df6696dd828f67ccd6dd39ccaa270ae1fae1f67a8d1b6596800846cf0cfb6e` |
| `tests/ai.test.mjs` | NEW (untracked; created Checkpoint A) | 1485 (`wc -l`), 131 `test()` declarations | `f894a34f6344f7f93da133324c35050aedbedb17356b3b48bda9608a68683e76` |
| `src-js/dsl.js` | MODIFIED (Checkpoint E only) | **+35/−11** | `e35b3132d4c274ce31d1482cdcfecb656b83c12c693bdf2ba829890d84708edd` (unchanged since E) |
| `scripts/run-suite.sh` | MODIFIED (registration only) | +2/−2 (header comment :5 + `FILES=` list :15) | `05bb64af35fdae2fca64a99a9bb2aa62db67d6d4c06e69bea421cef5d7bb95b1` |
| `scripts/phase3.13-evidence/` | NEW dir | 88 files (per-checkpoint RED/green/stub-kill/typecheck transcripts, pristine snapshots + sha256 sidecars, stub-kill runners, this report) | — |
| `scripts/probe-3.13-checkpointB-determinism.mjs` | NEW (untracked) | B-era §16 probe | — |
| `scripts/probe-3.13-checkpointG-scan.mjs` | NEW (untracked) | G-era scan probe | — |

Confirmed NOT touched:

- `src/core/` — 0 entries (`git diff --numstat origin/main -- src/core/` empty; `git status --porcelain src/core/` empty).
- No other `src-js/*.js` — `git diff --numstat origin/main -- src-js/` shows only dsl.js (+35/−11) plus new ai.js.
- `package.json` — no delta vs origin/main (byte-identical, `b329cc0d…`); zero dependency fields (package.json:1-9).
- All other `tests/*.mjs` — unmodified (git status: only run-suite.sh + dsl.js tracked-modified).

## Tests

- Total: **679** counted (15 `.mjs` files) + 5 standalone gates.
- Passed: **679** / 5 gates PASS.
- Failed: **0**. Exit 0 (`3.13-H-npm-test.txt`).
- Baseline preserved: **548** (sum of the 14 non-ai counted files in the H-run table; 679 − 131 ai = 548; dsl 55/55, tools 84/84, renderer 52/52, transaction 32/32, interaction 55/55, boundaries 13/13, semantic 49/49, scenegraph 41/41, constraints 40/40, appearance 36/36, geometry 37/37, appearance-graph 22/22, spatial-index 13/13, stores 19/19).
- Planner attribution (programmatically counted from `test(` declarations in tests/ai.test.mjs at Checkpoint H): **A 29 + B 30 + C 38 + D 9 + E 15 + F 4 + G 6 = 131** (see Deviations H-2 for the era-time attribution note).
- `npm run typecheck` — exit 0 (`3.13-H-typecheck.txt`).
- Note: spec §41-H projected baseline "570/570"; the measured pre-3.13 counted baseline was 548 (Checkpoint A baseline run `3.13-A-npm-test.txt`: 577 = 548 + 29). The user's Checkpoint H instruction (679/679 + 548) matches reality.

## Vertical Slice

Spec §20/§37/§41-F, proven by tests/ai.test.mjs F-S1..F-S4 over the real substrate (evidence: `3.13-F-green.txt`, `3.13-F-npm-test.txt`, `3.13-F-stubkill.txt`):

- **Intent**: the §20 slice intent — "create red rounded rect 200x100 radius 12, centered on artboard" (tests/ai.test.mjs:244, :1108-1109). Artboard SUPPLIED via `createPlanningContext` (§24 — never guessed).
- **ExpectedState**: mechanical derivation — rounded-rect geometry 200×100 rx12/ry12, solid red fill, spatial.centered true, status `'requested'` (ai.js:393 `buildExpectedState`; deep-frozen by ai.js:864 `createExpectedState`).
- **Plan**: 3 steps — T01 (geometry-only, NO fill key, Rule C) → T07 (`{objectIds:['$step-1'], fill:{kind:'solid',color}, opacity}`) → T08 (`{objectIds:['$step-1'], axis:'both', mode:'center'}`), `$stepN` dependency refs; plan deep-frozen, every toolId resolves in the live registry (ai.js:652-723 plan builder; ai.js:1153 registry validation).
- **DSL**: `compilePlanToDSL` → existing VectorDSL instructions [create/rect, appearance, align] with `$doc:`/`$step` prefixes stripped (ai.js:1340/:1237); parses + validates + compiles to IR through the REAL dsl.js (threaded context, Checkpoint E).
- **Tool Execution**: DSLExecutor.execute over the real registry; T01 tools.js:227 persists geometry (x/y top-left + rx/ry passthrough), T07 tools.js:348 writes the canonical fill stack item, T08 tools.js:389 align; mutation steps routed via `executeViaSubstrate` (tools.js:1044/:1069) — one real substrate transaction per executed step.
- **Transaction**: 3 steps → 3 substrate commits; history grows by exactly 3 (`HistoryManager` push-committed-only, transaction.js:159-173/:223); `TransactionCommitted` published strictly after each commit (transaction.js:441-449).
- **Commit**: output ids == TransactionCommitted event ids == history ids in commit order (existence/consistency proof; runtime UUID/Date.now envelopes deliberately outside the §16 claim — F disclosure 2, APPROVED).
- **Final State**: object exists in ObjectStore with resolvable `geometryRef`/`appearanceRef`; `GeometryStore.get(geometryRef).params` = {x:300, y:250, width:200, height:100, rx:12, ry:12}; AppearanceStore stack contains {kind:'solid', color:{r:255,g:0,b:0,a:1}, enabled, opacity 1} (canonical RGB via parseColor dsl.js:333-342 — NOT a flat `Fill === "#FF0000"` field, per §37); WorldBBox center (400,300) === artboard center within 1e-9; `spatialInvalidations === 3` (harness counters tests/ai.test.mjs:1160/:1179/:1246); RenderTree needsFullRebuild + rebuilt tree contains the object.
- **4-step proof outcome**: PASS — F-S1 deterministic snapshots captured; F-S2 planning chain left stores byte-identical, history unchanged, SpatialIndex/RenderTree NOT invalidated (Planner read-only end to end); F-S3 real pipeline executed; F-S4 committed-state assertions all green.

## Architecture Checks

Checkpoint G (spec §26/§41-G), all against `src-js/ai.js`, comment/string-aware scan with anti-vacuity guard (evidence: `3.13-G-clean-green.txt`, `3.13-G-stubkill.txt` + run1/run2/run3 transcripts):

| Check | Target (§41-G) | Result | Proof |
|---|---|---|---|
| G-1 | import contract | PASS | every specifier relative; set EXACTLY `['./tools.js']` (ai.js:55) — no fs/node:/bare npm. KILL 1: injected `import fs from 'node:fs'` → G-1+G-3+G-5 fail exactly as predicted |
| G-2 | window / document / fetch | PASS | zero code-position hits on stripped body. KILL 2: alias-form `void window;` → G-2 fails |
| G-3 | eval / Function (+ disclosed superset: require( / import( / globalThis / process / fs) | PASS | zero hits. KILL 2: `const evalAlias = eval;` (alias form, no parens) → G-3 fails — the A-era raw paren scan stays green on the same injection, proving the word-boundary scan catches what it structurally cannot |
| G-4 | Store.write / Transaction.execute / Renderer / SpatialIndex mutation (+ disclosed superset: register/unregister/invalidate/invalidateAll/insert/remove/update/delete/create/render/commit/rollback) | PASS | zero mutation-method calls. `.push/.add/.get/.has/.list` deliberately NOT denied (Planner-internal plain-data ops + the frozen {has,get,list} registry projection). KILL 3: dead probe.write()/execute()/commit()/insert()/render() → G-4 matches exactly those |
| G-5 | zero npm dependencies | PASS | package.json:1-9 declares no dependency fields; every src-js/*.js runtime import relative. KILL 1 non-relative import → G-5 fails |
| G-6 | anti-vacuity guard | PASS | stripper demonstrably removes the documented ai.js comment/string 'document' occurrences AND preserves code tokens (createCoreToolRegistry/PLANNER_TOOL_REGISTRY/deepFreeze/compilePlanToDSL), 1:1 char/line mapping — over-stripping cannot make G-2..G-4 pass vacuously |

3-kill stub-kill protocol: 3 injections into the scan target ai.js (test file never modified), each runtime-inert so only the corresponding assertions flip; restores sha256-verified ×3 (e3df6696… before == after each); post-restore 131/131 exit 0.

## Contract Compliance

- **R01 — Planner is Read/Plan Only (spec §02/R01)**: PASS. Structural: ai.js imports exactly one substrate module (ai.js:55) and contains zero mutation-method calls (G-4 :1445). Runtime: F-S2 proves byte-identical canonical stores, unchanged history.size(), SpatialIndex/RenderTree NOT invalidated across the full planning chain (tests/ai.test.mjs:1266). Registry reach is the frozen `{has,get,list}` projection (ai.js:65); D-6 tripwires prove createPlan/validatePlan/compilePlanToDSL never execute anything (:872).
- **R02 — AI Boundary (spec §02/R02)**: PASS. The only mutation path is Plan → DSL → Tool Registry (validation) → Transaction → Commit, executed by the substrate (F-S3 :1280); no direct AI→Store / AI→Renderer / AI→SceneGraph.write / AI→Transaction.execute surface exists (G-2/G-3/G-4 scans) and execution-side tools run only through the registry gate (tools.js:1044/:1069).
- **§16 determinism**: PASS. Content-derived ids (stableStringify ai.js:149 + fnv1a32 ai.js:159); byte-identical ExpectedState/Plan/DSL across runs asserted per rule (:471-487, :590, :642, :676, :732); B-era standalone probe hashes byte-identical across B/D/E/F/G re-runs (`3.13-G-determinism-regression.txt`: DSL 4ff9b828…1a366, plan-254f7217, intent-77a22f3a); zero entropy sources in ai.js (A-era source-wide scan :225 + G scans).
- **§13 planning context**: PASS. `createPlanningContext` deep-freezes and rejects function values (ai.js:566-596; tests :203-218); `createPlan`/`validatePlan` throw INVALID_PARAMETER on contract-violating contexts (ai.js:864/:1197; tests :281, :415).
- **§20 mandatory vertical slice**: PASS. The full slice intent executes end-to-end through Transaction/Commit (F-S3/F-S4 above); the §20 centering mandate is machine-enforced as a Planner rule (centered ExpectedState ⇔ T08 both/center, tests :383/:391, mandated comment in ai.js).
- **§22 commit verification**: PASS. Final state asserted through canonical stores by ref resolution (GeometryStore via geometryRef, AppearanceStore stack via appearanceRef) — the §37 canonical-form mandate, not flat fields (F-S4 :1292).
- **§23-§25 DSL integration**: PASS. §23 single DSL syntax — planner output compiles to the EXISTING grammar, no second DSL (test :558; ai.js:1340). §24 round trip — Plan → DSL → parse → validate → IR, same toolIds/inputs/targets for every rule A–H (:432, E-RT :1064-1079); artboard/context identity never guessed (ai.js:611, resolveDocRefs ai.js:739). §25 registry integration — every step resolves against the live registry with category/schema validation (D block :819-942).

## Regression

- All prior phases green: 14 non-ai counted files **548/548** (per-file table in Tests above; every file byte-unmodified per git status), 5/5 standalone gates PASS (geometry.test.js, architecture.test.mjs, architecture-phase3.04.mjs, run-js.js, src-js/run.js dsl vertical slice), `npm run typecheck` exit 0.
- dsl.test.mjs 55/55 UNMODIFIED over the Checkpoint E substrate extension (byte-untouched file — backward-compat mandate).
- §16 determinism hashes reproduced at D, E, F and G (`3.13-{D,E,F,G}-b-determinism-regression.txt`).
- Zero regressions from any prior phase; zero new files in the counted suite beyond the §30-mandated single planner test file (run-suite.sh:15).

## Known Issues

Only real, pre-existing, disclosed issues — none introduced by PHASE 3.13:

1. Recovery-era frozen backlog (mandated untouched in 3.13, verified untouched): G-1, G-2, T10, T11, T13, transaction.js:529-538, and dsl.js:559/:563 (DSLExecutor's disclosed direct-store delete fallback when T04 itself fails). None intersects the Planner seam (3.13-0 audit, worklog).
2. Substrate-side SpatialIndex invalidation on TransactionCommitted does not exist; it is wired at the composition layer (test harness mirrors renderer.js:317-321). PHASE G backlog (worklog Task 3.13-8).
3. A-era `architecture.test.mjs` raw paren scans cannot see alias-form capability references (`const x = eval; x()`); G-3's word-boundary scan catches them. PHASE G backlog: consider upgrading the A-era scan to the G pattern (worklog Task 3.13-10).
4. Execution-path `$doc:` integration: `compilePlanToDSL` strips `$doc:` prefixes (ai.js:1237), so the DSL executor env resolves only program-internal refs. Deliberate Checkpoint E non-goal (disclosed, APPROVED); integration deferred to PHASE G / 3.14 agenda.
5. DSL ARTBOARD op is accepted but not persisted to any store (dsl.js:461-463/:565-568) — pre-existing substrate behavior; the Planner's §20 centering does not depend on it (plan-time coordinates, Mapping deviation 3).

## Deviations

All documented at their checkpoint STOP reports and APPROVED by the user — none silent.

**Mapping + Checkpoint A (7 deviations, worklog Task 3.13-2):**
1. §12/§20 T07 input shape: spec `{target, fill:'#FF0000'}` vs tool-level `{objectIds[], fill:{kind:'solid',color:{r,g,b,a}}}` — Planner emits DSL-level string fill; DSL parseColor canonicalizes (dsl.js:333-342).
2. §12/§20 T08 `{targets,…}` vs tool-level `{objectIds,…}` — compliant via the DSL align op semantics (dsl.js:453-456, executor target resolution dsl.js:547-564).
3. §20/§22/§37 "centered on artboard": resolved at plan time via artboard-aware T01 coordinates; the §20-mandated T08 alignment step is retained (single-object align = documented no-op success, tools.js:390-395); committed WorldBBox center vs fixture artboard center asserted at F-S4 (1e-9).
4. §37 fill color asserted in canonical `{r:255,g:0,b:0,a:1}` form, not the `#FF0000` string.
5. §20 rounded rect: Planner emits rx=12 AND ry=12 (both persisted by T01, tools.js:232/:240).
6. Renumbering: user's earlier "§39 acceptance gates" are §36 in the delivered spec; §39 is the report template.
7. SpatialIndex commit-invalidation not pre-wired substrate-side — F harness wires it mirroring renderer.js:317-321 (spec §41-F allows the project's post-commit event = transaction.js:448).

**Checkpoint B (4 Planner rules, approved at B acceptance):** (a) opacity-only create (no fill) → NO_VALID_PLAN — T07 can never succeed on a fresh object (tools.js:377); honest refusal instead of a doomed plan. (b) placement center + explicit x/y → INVALID_PARAMETER conflict. (c) deep-frozen context contract — mutable context throws PlanningError (§13). (d) placement center without artboard → MISSING_PARAMETER (§24). Plus disclosure: suite file count stays 15 (§30 single planner test file) — user's "(file count will increase)" expectation superseded by the spec.

**Checkpoint C (disclosed, approved):** the DSL reference model pre-E knows only program-internal refs — standalone doc-referencing programs fail validateDSL; pinned as a test and resolved in Checkpoint E via approved Option A. New design: the `$doc:<objectId>` plan-level reference kind beside `$stepId` (uniform `$`-ref grammar, own-property reads).

**Checkpoint D (5 disclosures, ALL APPROVED):** (1) single substrate import + frozen `{has,get,list}` projection + `getPlannerRegistry`; (2) optional registry DI param on createPlan/validatePlan (duck-typed, backward compatible, determinism-neutral); (3) createPlan post-condition — unresolvable toolId refused with TOOL_NOT_FOUND; (4) validatePlan registry block incl. the NEW 12th error code TOOL_REGISTRY_INVALID + the mutation-only plan-steps rule (documented revisitable like the §20 center rule); (5) fixed JSON-schema subset validator with undeclared-keys-allowed semantics (matches the live schemas' deliberate under-declaration, tools.js:227/:232/:240); compilePlanToDSL untouched (mechanics).

**Checkpoint E (3 disclosures, ALL APPROVED):** (1) lintDSL and DSLExecutor.validate/compile remain single-arg (mandate-literal minimalism); (2) the DSL executor env resolves only program-internal refs — `$doc:` never reaches it because compilePlanToDSL strips prefixes; execution-path integration deferred (Known Issue 4); (3) E-6 malformed context keeps the gate CLOSED via the unknown-ref error — no throw added to the analyzer contract. Plus RED-ledger correction disclosed at the time (written prediction 8, honest count 9).

**Checkpoint F (2 disclosures, ALL APPROVED):** (1) SpatialIndex invalidation wired at the composition layer only (no substrate-side TransactionCommitted→index invalidation exists; harness mirrors renderer.js:317-321) — PHASE G backlog; (2) §16 snapshots exclude substrate-owned uuid/Date.now envelopes — §16 governs Planner outputs; runtime IDs proven by existence/consistency (outputs == events == history).

**Checkpoint G (4 honesty notes, ALL APPROVED):** (1) comment/string-stripping code-body scan with same-length placeholders (char/line 1:1, probe-validated); (2) patterns probe-validated before embedding — the squote/dquote fall-through infinite-loop bug was caught and fixed in the probe BEFORE embedding; (3) disclosed supersets (G-3 escape hatches; G-4 mutation-method surface beyond the literal targets; `.push/.add/.get/.has/.list` deliberately not denied — the frozen registry projection is read-only by construction); (4) G-5 spans all of src-js (project-wide zero-runtime-dependency rule).

**Checkpoint H (new observations, disclosed here):**
- **H-1**: The dsl.js delta is **+35/−11** — measured by `git diff --numstat origin/main` AND by GNU `diff -u` against the pristine snapshot (`dsl.js.checkpointD.pristine`, byte-identical to origin/main, sha 30371ab6…). The Checkpoint E evidence file `3.13-E-dsl-diff.txt` itself records 35/11; the "+36/−12" string in the E-era worklog prose/STOP summaries was a transcription slip at the time. Substance unaffected: dsl.js is byte-identical to the E-accepted state (sha `e35b3132…`, cross-verified in `3.13-F-baseline-dsl.txt` and `3.13-G-baseline-state.txt`).
- **H-2**: Test attribution by checkpoint, programmatically counted now: A 29 / B 30 / C 38 / D 9 / E 15 / F 4 / G 6 = 131. Era-time reports said "31 B + 14 E": at Checkpoint B the superseded boundary test counted under B; Checkpoint C rewrote it 1:1 under the C label (B 30 + C 38 thereafter), and the E-era suite line transcribed 14 where 15 E tests were appended (worklog Task 3.13-6 says "15 E tests" in its RED-first line). All era TOTALS (645/654/669/673/679) were correct; only the two attribution strings drifted.
- **H-3**: Spec §41-H's "Expected baseline: 570/570" was a projection; the measured pre-3.13 counted baseline is 548 (Checkpoint A baseline evidence). Final count 679 = 548 + 131, matching the user's Checkpoint H instruction.

## Acceptance Gate (§36)

Item-by-item, 27/27 PASS (spec lines 1213-1240; citations from source/evidence):

| # | Gate | Result | Evidence (file:line) |
|---|---|---|---|
| 1 | src-js/ai.js exists | PASS | src-js/ai.js (1350 lines; sha e3df6696…; git status untracked-new) |
| 2 | src/core remains untouched | PASS | `git diff --numstat origin/main -- src/core/` = 0 entries (H run) |
| 3 | Intent validation works | PASS | validateIntent src-js/ai.js:372; tests/ai.test.mjs:33-122 |
| 4 | ExpectedState works | PASS | ai.js:393/:441/:864; tests :124-158, :262-291 |
| 5 | Plan works | PASS | ai.js:506/:881; tests :159-176 |
| 6 | Plan validation works | PASS | ai.js:532/:921/:983/:1197; tests :186-202, :397-420, D :819-942 |
| 7 | Planner is deterministic | PASS | ai.js:149/:159; §16 tests :471-487 + per-rule :590/:642/:676/:732; probe hashes stable B→G |
| 8 | T01/T07/T08 integration works | PASS | F-S3 tests/ai.test.mjs:1280; T01 tools.js:227, T07 tools.js:348, T08 tools.js:389 |
| 9 | T01 receives no fill | PASS | buildCreatePlanSteps ai.js:652 (geometry-only); tests :306, :422 |
| 10 | Red rectangle rule works | PASS | buildAppearancePlanSteps ai.js:806; C-F tests :599-642; F-S4 fill :1292 |
| 11 | Rounded rectangle rule works | PASS | rx+ry emitted (test :306); F-S4 geometry rx:12/ry:12 :1292 |
| 12 | DSL compiler works | PASS | compilePlanToDSL ai.js:1340; single-syntax test :558 |
| 13 | DSL round-trip works | PASS | §24 test :432; E-RT :1064-1079 |
| 14 | Tool Registry integration works | PASS | ai.js:65/:1153/:1197; D tests :819-942 |
| 15 | Planner cannot mutate Stores | PASS | G-4 :1445; F-S2 byte-identical proof :1266 |
| 16 | Planner cannot execute Transactions | PASS | G-4 :1445; D-6 tripwire :872; F-S2 :1266 |
| 17 | No network | PASS | G-2 fetch scan :1429 (stripped code body) |
| 18 | No filesystem | PASS | G-3 fs scan :1435; G-1 node:/builtin ban :1421 |
| 19 | No browser dependency | PASS | G-2 window/document scan :1429 |
| 20 | Zero npm dependencies | PASS | G-5 :1453; package.json:1-9 (no dependency fields); all src-js imports relative |
| 21 | RED-first tests exist | PASS | 3.13-{A,B,C,D,E,F}-red-first.txt + 3.13-G-clean-green.txt (clean-run proof) |
| 22 | Stub-kill proof exists for critical rules | PASS | run-stubkill-checkpoint{A..G}.sh + transcripts; all restores sha256-verified (A ×1, B ×1, C ×2, D ×2, E ×1, F ×2, G ×3) |
| 23 | Vertical slice executes through Transaction | PASS | F-S3 :1280 — 3 steps → 3 substrate commits (tools.js:1044/:1069) |
| 24 | Commit changes canonical state | PASS | F-S4 :1292 — geometry/appearance via geometryRef/appearanceRef |
| 25 | History records transaction | PASS | F-S3 history +3 :1280; transaction.js:159-173/:223 |
| 26 | SpatialIndex invalidates after commit | PASS | F-S4 spatialInvalidations === 3 (harness :1179, assertion :1292) |
| 27 | RenderTree invalidates after commit | PASS | F-S4 needsFullRebuild + rebuilt tree contains object :1292 |

## Next Phase

PHASE 3.14 — CRITIC + EVALUATION.

Ready: **YES** — all 27 §36 gates PASS; the Planner API surface (ai.js exports) is stable and frozen-contract; every downstream seam the Critic needs (Plan verdicts with structured errors, ExpectedState, registry metadata) is implemented and regression-proven; all backlog items are phase-scoped (PHASE G substrate work / 3.14 execution-path `$doc:` integration) and none blocks 3.14. Working tree left uncommitted per H.5 — commit awaits explicit user approval after review of this report.
