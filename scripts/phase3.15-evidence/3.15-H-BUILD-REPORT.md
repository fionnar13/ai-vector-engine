# PHASE 3.15 BUILD REPORT — CORRECTION LOOP EXECUTION

**Status:** PASS (all 34 acceptance gates verified — see §70 gate table)
**Phase:** 3.15 — Correction Loop Execution (spec §0–§72)
**Branch:** `feat/phase-3.15-correction-loop` — HEAD `70fd6a9` = the Phase 3.14 committed baseline (merged to `origin/main` as `a99c9cb` via PR #3, verified at 3.15 grounding). Zero commits, zero pushes during the phase; all work lives in the working tree (repository-state discipline).
**Working tree:** `M scripts/run-suite.sh` (registration only; `git diff HEAD --stat` = 1 file changed, 1 insertion(+), 1 deletion(-)) + untracked new files (Section 1). Nothing else modified.
**Report date:** Checkpoint H. Every factual claim cites file:line. Evidence artifacts: `scripts/phase3.15-evidence/` (117 files: 113 from Checkpoints A–G + 4 produced by this checkpoint: `3.15-H-npm-test-final.txt`, `3.15-H-typecheck.txt`, `3.15-H-final-hashes.txt`, `3.15-H-probe.txt`).
**Mandate provenance (honesty note):** the 15-section template below is the Checkpoint H directive (the 3.13-H/3.14-H precedent). The 34-item gate battery is the verbatim §70 "Definition of Done" checkbox list. The README mandate is §55 (single-file house style + the module-content list) executed under the §53-mandate documentation tradition of 3.13/3.14 (in the 3.15 spec text, §53 is User Cancellation; §62 is the Regression Suite — both reported where they bind).

---

## 1. Implementation (files created/modified)

| File | Status | Size | Notes |
|---|---|---|---|
| `src-js/correction.js` | NEW (Checkpoints A–G; unchanged since the G-pristine) | 3335 lines, 76 exports | sha256 `9e31da8dff4be9b5eedea67dd2f7e425ada7e53d9338bef6e083bdeae2a00fe2` — byte-identical to the Checkpoint G accepted pristine (`3.15-G-pristine-hash.txt`); ZERO imports (pinned by G-1, `tests/correction.test.mjs:3615`) |
| `src-js/correction.README.md` | NEW (Checkpoint H, §55 module-content mandate) | — | documentation only; not imported by any module (§55 "single-file" rule unaffected) |
| `tests/correction.test.mjs` | NEW (A–G era tests) | 3768 lines, 144 tests | sha256 `c7560bf40d74b15ea93abc50318cb744eafd3074ec81de79511b084b40c18a65` — unchanged since Checkpoint G |
| `scripts/run-suite.sh` | MODIFIED — registration ONLY | 55 lines | `tests/correction.test.mjs` added to the `FILES=` list at `scripts/run-suite.sh:15` (+1 file in the list; diff = 1 insertion/1 deletion; same sanctioned precedent as 3.13-A/3.14-A). sha256 `34d21656bbc3059e8d917d6d27c0327c03ddb46e4d832647db973634c198a38f` vs the pre-3.15 baseline `66048cff…` (`3.15-A-baseline-hashes.txt`) |
| `scripts/phase3.15-evidence/` | NEW (evidence) | 117 files | per-checkpoint RED/GREEN/stub-kill/typecheck/baseline artifacts + 10 pristine snapshots + the F/G stub-kill battery scripts |
| `scripts/probe-3.15-checkpointG-scan.mjs` | NEW (G-era probe) | — | pre-embedding scan-pattern validation; re-run green at H (`3.15-H-probe.txt`, PROBE PASS) |
| `scripts/run-stubkill-checkpointF.sh`, `scripts/run-stubkill-checkpointG.sh` | NEW (battery runners) | — | F dual-kill and G six-kill batteries; predictions-written-first discipline (`3.15-F-stubkill-predictions.txt`, `3.15-G-stubkill-predictions.txt`) |
| `src/core/**` | UNTOUCHED | 178 files | `git status --porcelain src/` = 0 entries; recursive aggregate sha256 `137327739471ff095325854c296a82aa84afde0258b396ad6902b22de3e21e56` re-verified at H (`3.15-H-probe.txt` §5) and counted in-suite (G-11, `tests/correction.test.mjs:3731`) |
| all other `src-js/*.js` | UNTOUCHED | — | `git status --porcelain src-js/` shows only the untracked NEW `correction.js`; every tracked module is byte-identical to its 3.14-H accepted value: `ai.js` `e3df6696…`, `evaluation.js` `a719a433…`, `critic.js` `0ab9f7f0…`, `transaction.js` `5ec1369b…`, `tools.js` `0dc8124f…`, `dsl.js` `e35b3132…` (`3.15-H-final-hashes.txt` vs `3.15-A-baseline-hashes.txt`) |

Full sha256 records for all 19 `src-js` modules + 2 READMEs + `package.json` + `run-suite.sh` + the test file: `3.15-H-final-hashes.txt`.

## 2. Evaluation Model (not applicable — that's 3.14)

The Evaluation Model (ActualState / EvaluationResult / Deviation) was Phase
3.14's deliverable and is UNCHANGED here: `evaluation.js` = `a719a433…`,
`critic.js` = `0ab9f7f0…` (Section 1 hashes). The correction loop CONSUMES
§12 EvaluationResults and never builds one: `requireEvaluation` is a local
shape guard over the injected critic's records
(`src-js/correction.js:2129-2139`; field errors `:2106-2128`), and the
engine's agenda refresh reads observedValue/evidence FROM the current critic
evaluation (`refreshedAgendaTarget`, `correction.js:2950-2980`) without
re-deriving any §12/§13 vocabulary. Zero imports means the loop cannot even
construct an EvaluationResult — the authority stays in evaluation.js by
construction (header `correction.js:327-338`).

## 3. Correction Loop Engine (state machine, session)

- **State machine (§3/§5)** — 11 states `CORRECTION_LOOP_STATES`
  (`correction.js:661-665`); the frozen transition map
  `CORRECTION_LOOP_TRANSITIONS` (`:1238-1252`) holds the eleven §3 cycle
  edges + the five disclosed derived edges (header disclosures 2–4);
  `canTransition` `:1253`, `transitionSession` `:1259-1280` (returns a NEW
  frozen session; trail append pinned by stub-kill KILL-2 at Checkpoint A),
  `terminateSession` `:1281-1304` (§50 reason mandatory; §53 USER_CANCELLED
  overlay from any active state — test A-31, `tests/correction.test.mjs:714`).
  Illegal edges refuse with INVALID_TRANSITION naming both states (A-25
  `:648`); TERMINATED is absorbing (A-22 `:600`).
- **Session (§4)** — `createCorrectionLoopSession` `:1108-1160`, validator
  `:1161-1237`: content-derived `loop-` id over INITIAL content only
  (transition-invariant), `visitedStates` trail, §6 corrections trail,
  currentEvaluation defaulting to the initialEvaluation, TERMINATED⟺reason
  enforced in BOTH directions (disclosure 5).
- **Policy (§18)** — `POLICY_DEFAULTS` `:695-704` (maxIterations 5,
  maxConsecutiveNoProgress 3, minimumImprovementDelta 0.01,
  maximumRegression 0.05, preserveBestState / rollbackOnCriticalRegression /
  allowOscillationRecovery true), `createCorrectionLoopPolicy` `:705`,
  validator `:715-751`.
- **Attempt (§6)** — `createCorrectionAttempt` `:1030-1053`, statuses
  `CORRECTION_ATTEMPT_STATUSES` `:672-675`, validator `:1075-1105`; the
  attempt embeds its target and plan and carries the mandatory
  beforeEvaluation; afterEvaluation/delta are appended by acceptance — each
  status stage is a distinct immutable record (header `:87`).
- **Engine (§24/§26)** — `CorrectionEngine` `:3329-3335` = the five pure
  operations `start/iterate/run/terminate/rollback` (`:3208/:3244/:3273/
  :3293/:3304`) over immutable engine states; the §26 mode dispatch table
  `CORRECTION_MODE_DISPATCH` `:2751-2757` (AUTO cyclesPerRun null /
  GUIDED requiresApproval / SINGLE_STEP stepBudget 1); modes proven by
  E-5 (`tests/correction.test.mjs:2348`), E-11/E-12 (`:2458/:2491`),
  E-13/E-14 (`:2505/:2522`); the §44 engine-determinism proof E-19
  (`:2612`); engine records carry no uuid/Date.now entropy (disclosure 41).

## 4. Correction Diagnostics (target, diagnosis, strategy)

- **Target (§7)** — `createCorrectionTarget` `:754-774`, validator `:795`:
  11 categories `:676-680`, 4 severities `:681`, non-empty §28 evidence,
  confidence ∈ [0,1], content-derived `ctarget-` id.
- **Diagnosis (§8)** — the deterministic attribution table
  `CATEGORY_TO_ROOT_CAUSE` `:1305-1327` (11 keys, B-1
  `tests/correction.test.mjs:788`); `resolveCorrectionDiagnosis`
  `:1383-1427` produces the §8 diagnosis with capability-grounded
  recommendations (B-5 `:838`, full 11-category sweep B-6 `:861`,
  determinism B-7 `:890`). Unsupported categories resolve to the frozen
  NO_CAPABILITY record that CANNOT enter planning (B-10/B-11 `:914/:923`;
  the §28 chain-end discipline C-19 `:1497`).
- **Strategy (§9)** — `createCorrectionStrategy` `:884-901`, validator
  `:924`: risk ∈ {LOW, MEDIUM, HIGH} `:689`, reversibility flag,
  confidence ∈ (0,1] strictly positive (disclosure 7; ranking viability
  B-19 `:1018`).
- **Ranking (§19)** — `rankCorrectionStrategies` `:1502-1542`: exact
  deterministic priority = Severity × Confidence × Impact × Correctability
  with the frozen weights `:1361-1365` (B-16 exact arithmetic `:977`,
  multi-candidate order B-17 `:993`, stable tie-break B-18 `:1008`).
- **Impact/Cost/Explainability (§20/§38/§27)** — `computeCorrectionImpact`
  `:1437-1467` (LOCAL/SUBTREE/GLOBAL; B-12/B-13 `:933/:946`);
  `computeCorrectionCost` `:1468-1501` (deterministic integer, B-14
  `:957`); `explainCorrectionDiagnosis` `:1543-1564` and
  `explainCorrectionStrategySelection` `:1565-1600` produce frozen
  rationales with numeric factors attached (B-era explainability tests).

## 5. Correction Planning (plan, preview)

- **Command derivation** — `CORRECTION_CAPABILITY_RECIPES` `:1328-1353`;
  `deriveCorrectionCommands` `:1752-1798` materializes EXACT deltas
  (C-1 `tests/correction.test.mjs:1154`); the local input contract
  `CORRECTION_CAPABILITY_INPUT_CONTRACT` `:1601-1616` is proven necessary
  AND sufficient against the LIVE tool validators (C-18 `:1446`) — every
  generator-emitted command passes the live registry validators and every
  plan kind maps to a REAL transaction.js factory.
- **Safety gates (rule-based, §35–§37/§40/§41)** —
  `evaluateCorrectionSafety` `:1870-1914` rejects any plan whose command
  mutation axes (`TOOL_MUTATION_AXES` `:1667`, derivation `:1679-1722`)
  would move a pinned hard-constraint axis (`CONSTRAINT_PINNED_AXES`
  `:1648`); `verifyCorrectionScope` `:1915-1949` (C-era OUT_OF_SCOPE
  refusals C-13 `:1356`); `scanCorrectionDependencies` `:1799-1869`
  (dependency closure).
- **Plan (§10/§11)** — `generateCorrectionPlan` `:1956-2054`: emits the
  frozen §10 plan (`createCorrectionPlan` `:951`, validator `:986-1027`)
  with materialized commands, §43 RULE_BASED expectedImprovement, and the
  preserved constraint evidence (C-10 `:1300`, C-11 `:1327`); refuses with
  the frozen vocabulary `PLAN_REFUSAL_REASONS` = INSUFFICIENT_EVIDENCE /
  HARD_CONSTRAINT_REJECTED / OUT_OF_SCOPE `:1950-1955` (C-12 hard-constraint
  refusal `:1344`, C-13 `:1356`); coherence discipline C-14 `:1376`;
  determinism C-15 `:1394`.
- **Preview (§42 dry run)** — `buildCorrectionPreview` `:2055-2098`: exact
  affectedObjects + commands + §43 RULE_BASED expectedEvaluation + exact
  §38 cost, zero execution (C-16 `:1410`, input discipline C-17 `:1436`).

## 6. Execution (attempt, transaction, acceptance)

- **Per-attempt independent transaction (§12)** —
  `executeCorrectionAttempt` `:2492-2571`: ONE plan → ONE independent
  substrate transaction with a content-derived `atx-` id passed EXPLICITLY
  through `TransactionBuilder.begin({id})` (`:2528-2531`; disclosure 25),
  parentId = the session root (lineage, never nesting). Commands are
  wrapper Commands routing plan descriptors through the LIVE tools
  (`correctionCommandFor` `:2417-2463`; the module's only `tool.execute`
  lives inside the wrapper Command body — disclosure 26, pinned by G-6
  `tests/correction.test.mjs:3669`). Pre-flight refusals against the live
  registry (`executionPreflightRefusal` `:2464-2491`) return
  EXECUTION_REFUSED envelopes (`CORRECTION_EXECUTION_REFUSALS` `:2101`).
  Runtime tool failure fails the transaction ATOMICALLY — stores untouched,
  nothing pushed (substrate guarantee `transaction.js:225-228`) — surfacing
  as a FAILED attempt.
- **Acceptance (§14)** — `acceptCorrectionAttempt` `:2572-2674`: ACCEPTED
  only on strict post > pre improvement of the plan metric
  (`metricGapOf` `:2140-2159`), plus structural validity (D-15
  `tests/correction.test.mjs:1998`), §15 critical gate, and transaction
  validity; verdicts `CORRECTION_VERDICTS` `:2099`, rejection reasons
  `:2100` (D-13 NO_IMPROVEMENT `:1964`, D-14 CRITICAL_REGRESSION `:1980`).
- **Regression (§15)** — `detectCorrectionRegression` `:2314-2387`: frozen
  RegressionReport; criticality = target-metric regression OR new error
  deviations OR worsening beyond `maximumRegression` (`:2369-2373`);
  critical + `rollbackOnCriticalRegression` drives the §3 ROLLING_BACK
  disposition in-loop (E-16 `:2552`).
- **Best state (§16)** — `preserveCorrectionBestState` `:2282-2308` under
  the strict total order `isBetterEvaluation` `:2270-2281` (errors, then
  warnings, then canonical id tie-break): the incumbent survives worse
  candidates (D-16 `:2025`; integrated D-19 `:2112`).
- **Fingerprint/no-progress (§30/§33)** — `computeCorrectionFingerprint`
  `:2166-2181`, `computeEvaluationFingerprint` `:2182-2189`;
  `shouldSkipCorrection` `:2226-2255` SKIPs the Δ=0 re-application (D-17
  `:2057`).
- **Call accounting (F-45)** — the general form calls = 1 + iterations +
  rollbacks (E-5's iterations+1 at `:2348` is the rollback-free special
  case); the §22 critic-call accounting is asserted on the live path
  (F-1 `:2964`).

## 7. Rollback (inverse commands, snapshot fallback)

- **The engine never re-implements inverse machinery** —
  `rollbackCorrectionAttempt` `:2675-2715` rides the substrate's OWN undo
  API behind the top-of-history guard (`:2687-2700`: the ONLY history reach
  is the READ `historyManager.getTransactionToUndo`; undoing a foreign
  transaction is refused `:2693`).
- **Inverse-first, snapshot fallback (substrate-owned)** — the substrate
  builds inverse commands when every command carries `getInverse`, else
  falls back to the before-snapshot inverse (`transaction.js:214-218`); the
  undo path restores from snapshot via `createRestoreCommands`
  (`transaction.js:291-297`, factory `:298`). The engine's F-2 §59-B test
  proves the live undo restores the row (F-2 `tests/correction.test.mjs:2990`),
  and F-7 (§60-P2) proves BYTE-EXACT restoration on every rollback path —
  engine, command-inverse, AND snapshot-fallback (`:3149`).
- **Engine manual rollback (§24 `rollback`)** — `engineRollback`
  `:3304-3328` performs the REAL undo, reports ROLLED_BACK at the
  ROLLING_BACK position, and refreshes through the critic (E-17 `:2567`);
  a spent or empty engine refuses.
- **Linear history preserved (invariant 13)** — the HistoryManager stays a
  flat linear undo stack through the whole loop (D-18 `:2076`; F-5 §59-E
  `:3086`; F-11 §60-P6 `:3269` — including the undo-truncation SIGNATURE
  assertions that prove linearity is honest, not merely unrestored);
  session metadata rides WITHOUT topology change (F-12 §60-P7 `:3298`,
  builder `correction.js:2718-2732`).

## 8. Convergence (VERIFIED, NO_PROGRESS, OSCILLATION, MAX_ITERATIONS)

`detectCorrectionConvergence` (`correction.js:2828-2863`) is the engine's
judgment OVER the critic's records; kinds frozen at `:2746`
(`CORRECTION_CONVERGENCE_KINDS`):

- **VERIFIED (§17)** — agenda-scoped: every §7 target's metric gap is zero
  against the CURRENT critic evaluation (`agendaSatisfiedOf`
  `:2939-2941`, `firstUnsatisfiedTarget` `:2942`); a clean pass terminates
  with reason VERIFIED (E-5 `tests/correction.test.mjs:2348`; the F-1
  integration test closes the full pipeline to VERIFIED `:2964`).
- **NO_PROGRESS** — `detectCorrectionNoProgress` `:2190-2207`: the
  consecutive near-zero-delta streak against
  `maxConsecutiveNoProgress`; exhaustion maps deterministically
  (`exhaustionReasonFor` `:2981-2990`); proven live end-to-end by the §59-C
  integration test F-3 `:3022`.
- **OSCILLATION (§31)** — `detectCorrectionOscillation` `:2208-2225` over
  post-attempt fingerprints (rolled-back entries KEEP their post-attempt
  fingerprints — disclosure 40: a rollback restore is safety working, not
  A→B→A cycling); proven live by the §59-D test F-4 `:3062`; A→B→A
  evidence semantics carried from E-8.
- **MAX_ITERATIONS (§18)** — the hard budget: static face = the module's
  ONLY `while` is the guarded engineRun AUTO loop (`:3282-3289`, cap =
  `policy.maxIterations + 2` + the §17/§18 termination-invariant throw;
  no while(true)/for(;;)/do{ — G-8 `:3691`); dynamic face = E-18's
  48-scenario termination battery `:2583` (four authority behaviors ×
  three budgets — ALWAYS terminates within budget with a §50 reason) and
  the F-10 live-substrate termination grid `:3249`.
- **Termination reasons (§50)** — `CORRECTION_TERMINATION_REASONS`
  `:666-671`; every exit lands with a reason and a valid session
  (VERIFIED / NO_PROGRESS / OSCILLATION_DETECTED /
  MAX_ITERATIONS_REACHED / CONSTRAINT_BLOCKED / EXECUTION_ERROR /
  UNFIXABLE / FAILED / USER_CANCELLED vocabulary as disclosed).

## 9. Integration (Critic, Planner, Transaction)

- **Critic (§22/§23) — consumer-only authority** — the loop never
  self-evaluates: `validateCorrectionLoopRequest`
  (`correction.js:2767-2827`) refuses any request whose injected
  `critic.evaluate(document, context)` duck-type is missing (`:2800-2801`)
  and demands the §25 envelope; every evaluation (initial and
  per-iteration) is consumed VERBATIM from the critic (`:3089-3090`);
  convergence and acceptance are judgments OVER the critic's records
  (disclosure 35, header `correction.js:362-373`). Critic failures surface honestly —
  the loop records evidence, never fabricates corrections. The LIVE
  evaluation.js authority grounds the §23 duck-type (E-22 `:2670`), and
  the host critic adapter at multi-object scale merges per-target
  liveEvaluates through the LIVE `createEvaluationResult` — one-edge
  bridging, every record §12-valid (approved F-43; the F-era integration
  tests run on these LIVE records, F-1 `tests/correction.test.mjs:2964`).
- **Planner (§21)** — the disclosed re-entry discipline: corrections ride
  the SAME `validateIntent → createPlan → validatePlan` gate the Planner
  owns (grounding record, worklog 3.15-0); this module builds
  CorrectionPlans for its OWN attempts and never passes the Planner's gate
  for it; correction intents and proposals stay inert data across the
  boundary. The §61 golden scenario runs the REAL Planner over the three
  rectangles before any correction (F-13 `:3332`).
- **Transaction substrate** — the injected trio
  {registry, transactionManager, transactionBuilder} plus the read-only
  scene projection `findNodeByObjectId` (G-4 `:3655`) and the 16-prop
  WorkingCopy view mirror (G-6 `:3669`); every store transition is
  attributable to a committed history entry — no mutation without a
  transaction (F-6 §60-P1 `:3122`).
- **Renderer isolation (§33)** — the loop never renders; the renderer
  never mutates the document (F-8 §60-P3 `:3208`, byte-identical canonical
  stores across FRESH builders on the golden document; static face G-3
  `:3641` — render/renderer/RenderTree/invalidate absent from the module).
- **§63 Performance Requirements (incremental evaluation posture)** — the
  loop evaluates through the injected critic at the attempt positions and
  refreshes ONLY the working target from the current evaluation
  (`refreshedAgendaTarget` `correction.js:2950-2980`); the agenda checker
  is gap-scoped (`targetGapOf`/`agendaSatisfiedOf` `:2936-2941`) rather
  than whole-document; the affected-object/dependency closure derives from
  `scanCorrectionDependencies` `:1799-1869` (§41). Evaluation scope
  authority remains with the host critic per §23.

## 10. Architecture (static scans)

Checkpoint G suite (`tests/correction.test.mjs:3615-3768`, 12 tests
G-1…G-12) — the first COUNTED source scans correction.js has ever had
(the A-era zero-import pin became counted here). Comment/string-stripped
code body, word-boundary exact, alias-form capable, probe-validated
pre-embedding (`scripts/probe-3.15-checkpointG-scan.mjs`; re-run PROBE
PASS at H, `3.15-H-probe.txt` — stripper 1:1, clean-module zero matches,
allowlists exactly as pinned, controls all fire):

| Scan (§52/§66) | Test | Citation |
|---|---|---|
| import contract EXACTLY ZERO imports; no dynamic module escape (require(/import() | G-1 | `tests/correction.test.mjs:3615` |
| code body: no window/fetch/eval/Function/require/globalThis/process/fs/localStorage, alias-form aware; `document` scanned GLOBAL-REACH with the sanctioned §25 `request.document` carve-out (G-47: counted negative + positive controls) | G-2 | `:3622` |
| no store/renderer vocabulary; receiver-less mutation forms banned; bare `.set(/.delete(` pinned to the three LOCAL Map bindings (G-46) | G-3 | `:3641` |
| scene-graph boundary — only `findNodeByObjectId`; substrate shape exactly {registry, transactionManager, transactionBuilder, sceneGraph}; SceneGraph type absent | G-4 | `:3655` |
| HistoryManager boundary — the single sanctioned READ `getTransactionToUndo`; HistoryManager type absent from code | G-5 | `:3663` |
| sanctioned transaction-pipeline surface — receiver allowlists (transactionManager {execute,undo,historyManager}, transactionBuilder {begin,addCommand,build}, registry {get,has,validate}, tool {deterministic,execute,validate}, wc 16-prop view, ctx {constraints,scene,semantic,workingCopy}); no substrate machinery constructed | G-6 | `:3669` |
| no autonomous/deferred execution — setInterval/setTimeout/setImmediate/queueMicrotask/addEventListener/requestAnimationFrame/import(/new Promise all absent | G-7 | `:3684` |
| MAX_ITERATIONS boundedness — exactly ONE while (the guarded engineRun AUTO loop, region references policy.maxIterations + guard + the §17/§18 throw); SINGLE_STEP stepBudget; no unbounded form; dynamic face cited (E-18/F-10) | G-8 | `:3691` |
| no transaction-nesting API and no History DAG — vocabulary absent; every `.begin(` receiver is the injected transactionBuilder (per-attempt transactions are SIBLINGS via parentId lineage, disclosure 25) | G-9 | `:3704` |
| zero npm dependencies + whole-src-js relative-only import sweep | G-10 | `:3711` |
| src/core FROZEN — recursive 178-file manifest byte-identical (aggregate sha256, buffer==utf8 agreement); deterministic + change-sensitive on synthetic input; frozen files never killed | G-11 | `:3731` |
| anti-vacuity harness sanity — stripper preserves code AND removes comment/string occurrences, 1:1 char/line mapping (over/under-stripping both re-proven) | G-12 | `:3747` |

**Stub-kill proof:** six injected violations, ALL EXACT-MATCH FIRST-TRY,
zero self-corrections (`3.15-G-stubkill-run1..6.txt`, summary
`3.15-G-stubkill.txt`): KILL-G1 alias-form capability → {G-2}; KILL-G2
store/scene/history/renderer mutation → {G-3, G-4, G-5, G-6}; KILL-G3
timers/async + dynamic import → {G-1, G-7, G-10}; KILL-G4 nesting/DAG +
begin-receiver + construction → {G-5, G-6, G-9}; KILL-G5 while(true) →
{G-8}; KILL-G6 package.json dependencies → {G-10}. Every scan family
bitten; G-11 (frozen tree) and G-12 (harness sanity) proven in-test per
the 3.14-G-9 precedent. Per-checkpoint batteries: A (3 kills), B (dual),
C (dual), D (triple), E (E1+E2), F (dual) — all EXACT-MATCH with
sha256-verified restores (evidence dir).

## 11. Tests (actual totals)

`npm test` → **COUNTED SUITE: 943 passed / 0 failed (of 943)**, **5/5
standalone gates PASS**, exit 0 — evidence `3.15-H-npm-test-final.txt`:

| Test file | Total | Passed | Failed |
|---|---|---|---|
| tests/ai.test.mjs | 131 | 131 | 0 |
| tests/appearance-graph.test.mjs | 22 | 22 | 0 |
| tests/appearance.test.mjs | 36 | 36 | 0 |
| tests/constraints.test.mjs | 40 | 40 | 0 |
| **tests/correction.test.mjs (3.15)** | **144** | **144** | **0** |
| tests/dsl.test.mjs | 55 | 55 | 0 |
| tests/evaluation-critic.test.mjs | 120 | 120 | 0 |
| tests/geometry.test.mjs | 37 | 37 | 0 |
| tests/interaction-boundaries.test.mjs | 13 | 13 | 0 |
| tests/interaction.test.mjs | 55 | 55 | 0 |
| tests/renderer.test.mjs | 52 | 52 | 0 |
| tests/scenegraph.test.mjs | 41 | 41 | 0 |
| tests/semantic.test.mjs | 49 | 49 | 0 |
| tests/spatial-index.test.mjs | 13 | 13 | 0 |
| tests/stores.test.mjs | 19 | 19 | 0 |
| tests/tools.test.mjs | 84 | 84 | 0 |
| tests/transaction.test.mjs | 32 | 32 | 0 |
| **COUNTED SUITE** | **943** | **943** | **0** |

- **Phase 3.13 baseline preserved (§62)**: 943 − 144 − 120 = **679** —
  exactly the accepted 3.13 baseline (per-file evidence:
  `scripts/phase3.13-evidence/3.13-H-npm-test.txt`).
- **Phase 3.14 baseline preserved (§62)**: 943 − 144 = **799** — exactly
  the accepted 3.14 baseline (`scripts/phase3.14-evidence/3.14-H-npm-test-final.txt`).
  No regression was masked: every pre-3.15 file's pass count equals its
  accepted count (Section 11 table).
- **3.15 internal totals** (144 = 32+25+20+20+22+13+12): A-era 32
  (A-1…A-32), B-era 25 (B-1…B-25), C-era 20 (C-1…C-20), D-era 20
  (D-1…D-20), E-era 22 (E-1…E-22), F-era 13 (F-1…F-13: §59 Tests A–E,
  §60 Properties 1–7, §61 golden), G-era 12 (G-1…G-12).
- **Checkpoint progression** (all counted-suite runs, each with its own
  evidence file): 799 (pre-3.15) → 831 (A) → 856 (B) → 876 (C) → 896 (D)
  → 918 (E) → 931 (F) → 943 (G) → 943 (H, this checkpoint).
- Gates: `tests/geometry.test.js`, `tests/architecture.test.mjs`,
  `tests/architecture-phase3.04.mjs`, `tests/run-js.js`,
  `src-js/run.js` (dsl vertical slice) — all PASS
  (`3.15-H-npm-test-final.txt`).

## 12. Typecheck (actual result)

`npm run typecheck` → **exit 0** (tsc --noEmit, no errors) — evidence
`3.15-H-typecheck.txt`.

## 13. Dependencies (actual dependency diff)

- **Diff: zero.** `package.json` declares NO `dependencies`,
  `devDependencies`, `optionalDependencies`, or `peerDependencies` fields
  (verified by G-10 `tests/correction.test.mjs:3711` and by the probe
  re-run at H, `3.15-H-probe.txt` §6).
- `package.json` sha256 `b329cc0d410cef4fe81fa75deec5f2ad7655974c2ecdda13c1f669a12e8da965`
  — byte-identical to the pre-3.15 baseline (`3.15-A-baseline-hashes.txt`)
  and to the 3.14-H accepted value; untouched all phase
  (`3.15-H-final-hashes.txt`).
- Every `src-js/*.js` module imports only relative specifiers with no
  dynamic escape (G-1/G-10; the whole-tree sweep re-proven for 3.15).

## 14. Documentation (README per the §53-mandate tradition / §55 content list)

- **NEW `src-js/correction.README.md`** (Checkpoint H): documents every
  §55 module-content item with file:line citations — state machine (§2 of
  the README), session + policy (§3), attempt + target (§4), diagnosis
  (§5), strategy + ranking + impact/cost + explainability (§6), plan +
  preview (§7), hard-constraint/scope/dependency safety (§8), execution +
  acceptance + regression + best state (§9), fingerprints/no-progress/
  oscillation (§10), rollback (§11), convergence + modes + orchestration
  (§12), audit metadata §45/§46/§47/§48 (§13), integration §21–§25 (§14),
  determinism + architecture pins (§15).
- **Module header** (`correction.js:1-530`): the Checkpoint A–G scope
  notes and the 49 ratified disclosures (1–45 + G-46…G-49) — the
  spec-vocabulary decisions and their file:line anchors.
- House style mirrors `src-js/evaluation.README.md` /
  `src-js/critic.README.md` (the 3.14 §53 mandate); documentation-only —
  not imported by any module, so the G-1/G-10 import pins are unaffected.

## 15. Deviations (every deviation from this specification)

1. **`scripts/run-suite.sh` touched** — sanctioned by the §55 Suite
   Registration clause ("+1 line registration") and the build directive;
   registration ONLY (diff = 1 insertion/1 deletion at `:15`; byte-stable
   since Checkpoint A).
2. **G-46 execution-module scan calibration (ratified)** — the directive's
   "no Transaction.execute / Tool.execute" is operationalized for the
   §11/§12 EXECUTION engine as receiver-pinning allowlists instead of the
   3.14 read-only blanket bans; bare receiver-less forms banned outright;
   bare `.set(/.delete(` pinned to the three LOCAL Map bindings
   (header `correction.js:486-507`; approved at Checkpoint G acceptance).
3. **G-47 `request.document` carve-out (ratified)** — §25 sanctions
   `document` as the request FIELD; the scan forbids the GLOBAL-REACH
   form and carves out the property access with counted controls
   (approved disclosure 33 + G-47).
4. **G-48 §66 operationalization (ratified)** — boundedness static face
   = exactly ONE while + SINGLE_STEP stepBudget + unbounded-form bans;
   dynamic face cited (E-18 48-seed, F-10); HistoryManager read-pin;
   nesting/DAG pins (approved).
5. **G-49 freeze regeneration discipline (ratified)** — the 178-file
   manifest aggregate with buffer==utf8 agreement; frozen files never
   killed (approved).
6. **F-42…F-45 (ratified at Checkpoint F acceptance)** — the golden
   arrangement (planner-placed row + Rule G statement + spatial.bbox
   pinning); the host critic adapter's one-edge bridging; the §61 "Align
   command" as house MoveObject with the engine-driven T08 route surfaced,
   NOT patched (F-44, same tradition as 3.14-C 39(b)); the §22 call
   accounting general form (F-45).
7. **Two DISCOVERED CONTRACT GAPS surfaced, not patched (E-era, ratified)**
   — (a) the LIVE evaluator's bare `opacity` vs the house dotted
   `appearance.opacity` metric vocabulary (a house metric would
   false-VERIFY; honestly UNFIXABLE in the live metric; host adapter
   proves end-to-end closability — E-22 `:2670`); (b) the T11 recipe's
   object-id vs the live tool's group-node-id requirement (deterministic
   runtime FAILED; no-progress evidence — never a fabricated success).
   Both are recorded PHASE-G backlog (Section: PHASE G BACKLOG
   VERIFICATION).
8. **Report-template provenance note** — the 3.15 spec's §63 is
   Performance Requirements (reported under Section 9); the 15-section
   build-report template followed here is the Checkpoint H directive
   (3.13-H/3.14-H precedent). §53 in the 3.15 numbering is User
   Cancellation (implemented: `terminateSession` overlay
   `correction.js:1292-1304`, test A-31); the README follows the §55
   mandate under the 3.14 §53 documentation tradition. Recorded to keep
   every section reference auditable against the actual spec text.
9. **No other deviations.** No spec text weakened, no architectural
   boundary relaxed, no test result fabricated, no citation invented.
   §71 Non-Goals respected: no full constraint/semantic inference, no new
   memory architecture, no gradient mesh/3D/typography/rendering-engine
   work, no Illustrator cloning, no History DAG, no nested-transaction
   architecture (G-9 `tests/correction.test.mjs:3704`).

---

# ACCEPTANCE GATES (spec §70 Definition of Done) — 34/34 verified

The 34 items are the VERBATIM §70 checkbox list. Verdicts cite the
enforcing source construction (with line) and the enforcing test (with
line). All 144 3.15 tests + the full 943-test suite green
(`3.15-H-npm-test-final.txt`); no gate is unverified, none assumed.

| # | §70 Gate | Verdict | Evidence |
|---|---|---|---|
| 1 | src-js/correction.js implemented | **PASS** | `src-js/correction.js:1-3335` (76 exports); sha `9e31da8d…` = G-pristine; `3.15-H-final-hashes.txt` |
| 2 | tests/correction.test.mjs implemented | **PASS** | 3768 lines, 144 tests A-1…G-12; sha `c7560bf4…`; `3.15-H-npm-test-final.txt` (144/144) |
| 3 | scripts/run-suite.sh registration added | **PASS** | `scripts/run-suite.sh:15` (+1 file in FILES list; diff 1 ins/1 del); counted by the suite run |
| 4 | CorrectionLoopEngine implemented | **PASS** | `CorrectionEngine` `src-js/correction.js:3329-3335` (§24 five ops `:3208/:3244/:3273/:3293/:3304`); E-5 `tests/correction.test.mjs:2348` |
| 5 | State machine implemented | **PASS** | 11 states `:661-665`; transitions `:1238-1252`; `transitionSession` `:1259`; tests A-22 `:600`, A-25 `:648`, A-31 `:714` |
| 6 | Critic integration implemented | **PASS** | §22/§23 consumer-only `:2800-2801`, `:3089-3090`; E-22 `:2670`; F-1 `:2964` |
| 7 | Diagnosis layer implemented | **PASS** | `CATEGORY_TO_ROOT_CAUSE` `:1305-1327`; `resolveCorrectionDiagnosis` `:1383-1427`; B-1 `:788`, B-6 `:861` |
| 8 | Correction planning implemented | **PASS** | `generateCorrectionPlan` `:1956-2054`; `deriveCorrectionCommands` `:1752-1798`; C-10 `:1300`, C-18 `:1446` |
| 9 | Transaction integration implemented | **PASS** | `executeCorrectionAttempt` `:2492-2571` (`TransactionBuilder.begin({id})` `:2528-2531`); D-19 `:2112`; F-6 `:3122` |
| 10 | Session-local iteration history implemented | **PASS** | session trail `:1108-1160`; iteration ledger (disclosure 40, header `:424-434`); metadata builder `:2718-2732`; F-12 `:3298` |
| 11 | HistoryManager remains linear | **PASS** | D-18 `tests/correction.test.mjs:2076`; F-5 §59-E `:3086`; F-11 §60-P6 `:3269` (truncation signature); G-5 `:3663` |
| 12 | Regression detection implemented | **PASS** | `detectCorrectionRegression` `:2314-2387` (criticality `:2369-2373`); D-14 `:1980`; E-16 `:2552` |
| 13 | Convergence detection implemented | **PASS** | `detectCorrectionConvergence` `:2828-2863`; kinds `:2746`; F-1 VERIFIED `:2964` |
| 14 | Oscillation detection implemented | **PASS** | `detectCorrectionOscillation` `:2208-2225`; F-4 §59-D `:3062` |
| 15 | Best-state preservation implemented | **PASS** | `preserveCorrectionBestState` `:2282-2308`; strict order `:2270-2281`; D-16 `:2025` |
| 16 | Rollback implemented | **PASS** | `rollbackCorrectionAttempt` `:2675-2715` (top-of-history guard `:2687-2700`); `engineRollback` `:3304-3328`; E-17 `:2567` |
| 17 | Hard constraint safety implemented | **PASS** | `evaluateCorrectionSafety` `:1870-1914` (`CONSTRAINT_PINNED_AXES` `:1648`); C-12 `:1344`; F-9 §60-P4 `:3218` |
| 18 | Single-step mode implemented | **PASS** | `CORRECTION_MODE_DISPATCH` `:2751-2757` (SINGLE_STEP stepBudget 1); E-13 `:2505` |
| 19 | Guided mode implemented | **PASS** | GUIDED requiresApproval + pendingProposal (disclosure 38, header `:397-410`); E-11 `:2458`, E-12 `:2491` |
| 20 | Auto mode implemented | **PASS** | AUTO iterate-to-termination (the guarded while `:3282-3289`); E-5 `:2348`; E-18 48-seed `:2583` |
| 21 | Audit logging implemented | **PASS** | immutable stage records (header `:87`); per-call lastReport actions (disclosure 38, `:3013-3325` sites); §47/§48 metadata `:2718-2732`; D-20 `:2145` |
| 22 | Explainability implemented | **PASS** | `explainCorrectionDiagnosis` `:1543-1564`; `explainCorrectionStrategySelection` `:1565-1600` (§27) |
| 23 | Unit tests complete | **PASS** | A+B+C+D eras = 97 unit tests (`:1-2144` file ranges); 831/831 at Checkpoint D (`3.15-D-npm-test-final.txt`) |
| 24 | Integration tests complete | **PASS** | F-1…F-5 (§59 Tests A–E) `:2964/:2990/:3022/:3062/:3086` on the LIVE substrate; E-era engine integration E-5…E-22 |
| 25 | Property tests complete | **PASS** | F-6…F-12 (§60 Properties 1–7) `:3122/:3149/:3208/:3218/:3249/:3269/:3298`; E-18 48-seed `:2583` |
| 26 | E2E golden scenario complete | **PASS** | F-13 (§61) `:3332` — 3 rectangles → planner row → alignment error → critic detects → engine corrects → re-evaluates → verifies |
| 27 | Existing 3.13 tests pass | **PASS** | 679 = accepted 3.13 baseline (`scripts/phase3.13-evidence/3.13-H-npm-test.txt`); 943−144−120 = 679 (`3.15-H-npm-test-final.txt`) |
| 28 | Existing 3.14 tests pass | **PASS** | 120/120 evaluation-critic; 799 = accepted 3.14 baseline (`scripts/phase3.14-evidence/3.14-H-npm-test-final.txt`) |
| 29 | No renderer mutation introduced | **PASS** | static G-3 `:3641` (render/renderer/invalidate absent); dynamic F-8 §60-P3 `:3208` |
| 30 | No direct SceneGraph mutation introduced | **PASS** | static G-4 `:3655` (read-only `findNodeByObjectId` projection only); substrate commit is the sole mutation path (`transaction.js` commit `:407-439`) |
| 31 | No transaction nesting introduced | **PASS** | per-attempt transactions are SIBLINGS via parentId lineage (disclosure 25; `:2528-2531`); G-9 `:3704`; D-18 `:2076` |
| 32 | No History DAG introduced | **PASS** | G-9 `:3704` (vocabulary absent, linear invariant cited); F-5/F-11 truncation signature `:3086/:3269` |
| 33 | src/core remains untouched | **PASS** | `git status --porcelain src/` = 0; 178-file aggregate `13732773…` (`3.15-H-probe.txt` §5); G-11 `:3731` |
| 34 | All critical paths are stub-free | **PASS** | every checkpoint killed its stubs and restored sha-verified pristine: A (3 kills), B (dual), C (dual), D (triple), E (dual), F (dual), G (6/6) — all EXACT-MATCH (`3.15-{A..G}-stubkill*.txt`); final module = G-pristine `9e31da8d…` |

**§62 baseline clause:** the 16 pre-3.15 counted files sum to 799 = the
accepted Phase 3.14 baseline = 679 (3.13) + 120 (3.14); the 17 counted
files sum to 943. No regression was masked because no regression exists —
every pre-3.15 file's pass count equals its accepted count.

---

# PHASE G BACKLOG VERIFICATION (per H.6)

## 1. The pre-3.15 PHASE-G backlog was NOT silently absorbed

Non-absorption is proven structurally: every carrier artifact of a
recorded backlog item is byte-identical to its accepted pre-3.15 state
(`git diff HEAD --stat` = run-suite.sh only; hashes in
`3.15-H-final-hashes.txt` vs `3.15-A-baseline-hashes.txt`). Item-level,
as recorded in `/home/z/my-project/worklog.md`:

| # | Backlog item (worklog Task ID) | Carrier | Non-absorption proof |
|---|---|---|---|
| 1 | T13 orphan geometry/appearance store-GC policy (3.11/3.12 PHASE D item 2) | `stores.js` | sha `7c034936…` = pre-3.15 value; no GC policy added |
| 2 | T11 substrate ordering under nested groups (PHASE D item 3) | `tools.js` | sha `0dc8124f…` unchanged; ordering not modified |
| 3 | T10 createGroup array-as-parentId quirk (PHASE D item 4) | `tools.js` | sha unchanged; legacy branch NOT "fixed" |
| 4 | transaction.js:529-538 createTransformObjectCommand no-op (PHASE D item 5) | `transaction.js` | sha `5ec1369b…` unchanged; factory not rewritten |
| 5 | T14 real outline when a font system exists (PHASE D item 6) | `tools.js` | sha unchanged |
| 6 | T17 parametric-input kernel-bypass revisit (PHASE D item 7) | `dsl.js` | sha `e35b3132…` unchanged |
| 7 | semantic 'heading'→'title' alias mapping (PHASE D item 8) | `semantic.js` | sha `e00419fa…` unchanged |
| 8 | U1 residual dsl.js fallback direct-delete (PHASE D item 9) | `dsl.js` | sha unchanged; fallback still at `dsl.js:559/:563` |
| 9 | create-with-opacity residual (3.11/3.12-F → PHASE G) | `appearance.js`/`tools.js` | sha `5694ae53…`/`0dc8124f…` unchanged |
| 10 | G-1 frozen recovery item (3.11/3.12 frozen list) | tracked recovery tests | all tracked test files unmodified (`git diff HEAD` = run-suite.sh only) |
| 11 | G-2 orphan test-file residual `tests/interaction.test_1.mjs` (recovery F.1) | that file | untouched (untracked-file set contains only the six accounted 3.15 entries; `git status`) |
| 12 | SpatialIndex invalidation on TransactionCommitted → substrate (3.13-8) | `scenegraph.js`/`transaction.js` | sha `39fcea94…`/`5ec1369b…` unchanged |
| 13 | A-era raw paren scans miss alias forms — upgrade to G pattern (3.13-10) | `tests/ai.test.mjs` | sha `f894a34f…` = the 3.13/G accepted value (cited in `3.14-H-BUILD-REPORT.md` §KNOWN-PHASE-G); NOT upgraded |
| 14 | A-30 lacks `fs` (3.14-G) | `tests/evaluation-critic.test.mjs:547-557` | sha `a5bd6f83…` = 3.14-H value; pattern list re-read at H: window/document/fetch(/eval(/new Function/require(/import(/globalThis/process/localStorage — NO `\bfs\b` entry |
| 15 | A-31 lacks `.write(`/lowercase `.set(` (3.14-G) | `tests/evaluation-critic.test.mjs:558-568` | same file hash; pattern list re-read at H: `.create(/.update(/.delete(/.setX(/.commit(/.execute(/.register(/.unregister(/.insert(/.remove(/.render(/.rollback(` — NO `.write(`, no lowercase `.set(` |
| 16 | buildActualState bbox dispatch → path/star/polygon (3.14-A disclosure 1) | `evaluation.js` | sha `a719a433…` unchanged |
| 17 | nested-parent transform-delta re-derivation (3.14-C disclosure 4) | `evaluation.js`/`ai.js` | sha `a719a433…`/`e3df6696…` unchanged |

**Count disclosure (honesty note):** the H directive references "the 14
PHASE G items (from 3.14)". The worklog record on this side holds the 17
itemizations above (PHASE-D items 2–9, the two frozen G items, the
3.11/3.12 G-backlog carryover, the two 3.13 notes, and the four 3.14
notes); several records overlap (13/14/15 are one gap family across three
artifact generations; 3/4/8 appear in both the frozen list and the PHASE-D
consolidation). The non-absorption proof is ITEM-LEVEL and byte-exact, so
it holds for any subset or superset of the acceptor's canonical list:
nothing recorded as backlog was implemented in 3.15, and no tracked
carrier file changed at all.

## 2. The two 3.15-specific backlog items (recorded at E acceptance, worklog Task ID F-G1) remain OPEN

1. **Live evaluator bare `opacity` vs house dotted `appearance.opacity`**
   — surfaced by E-22 (`tests/correction.test.mjs:2670`), documented in the
   module header (disclosure 41(a), `src-js/correction.js:439-447`);
   the loop surfaces the honest vocabulary gap and the host adapter closes
   the loop end-to-end — NOT patched in production.
2. **T11 recipe object-id vs group-node-id** — surfaced as the
   deterministic runtime FAILED + no-progress evidence (disclosure 41(b),
   header `:448-452`); never a fabricated success — NOT patched.

Both were ratified at the E/F acceptances and recorded verbatim in the
worklog (Task ID F-G1); both remain on the PHASE-G backlog, joined by the
F-44 T08 boundary (the engine-driven T08 route is the disclosed
capability-table boundary, surfaced in F-13 step 5c — never patched).

---

# FINAL VERIFICATION MATRIX (repository state + §62)

| Requirement | Result | Evidence |
|---|---|---|
| Repository state: zero commits/pushes during 3.15 | PASS | HEAD `70fd6a9` on `feat/phase-3.15-correction-loop` (the 3.14 committed baseline); `git diff HEAD --stat` = 1 file (run-suite.sh, registration); new work = untracked files only |
| Source boundary: `src/core/**` untouched | PASS | porcelain = 0 entries; 178-file aggregate `13732773…` re-verified at H (`3.15-H-probe.txt` §5); G-11 in-suite |
| Runtime dependencies: zero | PASS | no dependency fields (`3.15-H-probe.txt` §6); G-10 `:3711`; package.json hash unchanged (Section 13) |
| Tests: total / passed / failed | PASS | 943 / 943 / 0 + 5/5 gates, exit 0 (`3.15-H-npm-test-final.txt`); 3.13 = 679 and 3.14 = 799 baselines preserved (§62) |
| Typecheck: exit 0 | PASS | `3.15-H-typecheck.txt` |
| Architecture scans pass | PASS | G-1…G-12 green in-suite; probe re-run PROBE PASS at H (`3.15-H-probe.txt`); 6/6 stub-kill EXACT-MATCH (`3.15-G-stubkill.txt`) |
| Determinism: repeated runs → identical | PASS | E-19 engine determinism `:2612`; D-20 `:2145`; C-15 `:1394`; B-7 `:890` |
| Mutation proof: the loop mutates only through committed transactions | PASS | F-6 §60-P1 `:3122`; F-7 §60-P2 byte-exact restore `:3149`; F-8 renderer purity `:3208`; static G-3/G-4/G-5/G-6 |
| Golden scenario (§61) | PASS | F-13 `:3332` — the full nine-step §61 pipeline on the LIVE substrate (planner row → alignment error → critic → diagnosis → correction → re-evaluation → decrease → verification) |
| §70 Definition of Done | PASS | 34/34 (table above); zero unverified gates declared |

Phase 3.15 is complete per §72: the system now spans AI Planning +
Transactional Execution + Evaluation + Diagnosis + Correction +
Verification — demonstrated by tests, not merely described by code.
