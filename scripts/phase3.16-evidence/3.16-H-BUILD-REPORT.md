# PHASE 3.16 — FINAL BUILD REPORT (CHECKPOINT H, PROMPT H-1)

Delivered: 2026-09-24 (UTC). Tree: `feat/phase-3.15-correction-loop` @ `cd5494d`
(0 commits beyond HEAD). Scope of this checkpoint: **verification only** —
zero production or test edits. NO commit, NO push (per directive).

Evidence files (this directory):
`3.16-H-full-suite-final.txt`, `3.16-H-inference-suite-final.txt`,
`3.16-H-typecheck-final.txt`, `3.16-H-core-probe.txt`,
`3.16-H-final-hashes.txt`, `3.16-H-snapshot.txt`, plus the pre-existing
`3.16-red.txt` / `3.16-green-file.txt` (RED/GREEN history).

---

## PART I — CHECKPOINT H VERIFICATION (H-1 … H-10)

### H-1 — Full test suite: **1049 GREEN**

| Run | Command | Observed | Exit |
|---|---|---|---|
| Counted suite | `bash scripts/run-suite.sh` | **943/943** passed, 0 failed + **5/5 gates** PASS | 0 |
| Inference suite | `node --test tests/constraint-inference.test.mjs` | **106/106** passed, 0 failed | 0 |
| Typecheck | `npm run typecheck` | no errors | 0 |

Counted-suite breakdown (17 files): ai 131, appearance-graph 22, appearance 36,
constraints 40, correction 144, dsl 55, evaluation-critic 120, geometry 37,
interaction-boundaries 13, interaction 55, renderer 52, scenegraph 41,
semantic 49, spatial-index 13, stores 19, tools 84, transaction 32 = **943**.
Gates: geometry.test.js, architecture.test.mjs, architecture-phase3.04.mjs,
run-js.js, src-js/run.js vertical slice — all PASS.

**Total: 943 + 106 = 1049 GREEN.** Matches the directive's expectation
exactly. Arithmetic chain with prior phase baselines: 679 (3.13) + 120 (3.14
file) = 799; 799 + 144 (3.15 file) = 943; 943 + 106 (3.16 file, run separately
per the H-1 protocol — `run-suite.sh` intentionally keeps its 3.15 file list,
see Discrepancy D-4) = **1049**.

### H-2 — Architecture scans (final): **ALL GREEN**

Every named scan ran inside a GREEN suite in H-1. Locations re-verified by
grep on the final tree (prompt-quoted line refs corrected where stale):

| Scan | Location | Status |
|---|---|---|
| A-2 category/severity enum | tests/evaluation-critic.test.mjs:198 | GREEN |
| B-29 tolerance key set | tests/evaluation-critic.test.mjs:878 | GREEN |
| B-30 import contract | tests/evaluation-critic.test.mjs:883 | GREEN |
| A-30 raw forbidden-capability scan | tests/evaluation-critic.test.mjs:547 | GREEN |
| A-31 raw mutation-surface scan | tests/evaluation-critic.test.mjs:558 | GREEN |
| A-32 'measured' escape-hatch scan | tests/evaluation-critic.test.mjs:569 | GREEN |
| G-1 evaluation.js import/dynamic-escape | tests/evaluation-critic.test.mjs:2154 | GREEN |
| G-3 evaluation.js mutation vocabulary | tests/evaluation-critic.test.mjs:2180 | GREEN |
| C-3 critic import/read-only/entropy | tests/evaluation-critic.test.mjs:915 | GREEN |
| F-3 critic tool/transaction surface | tests/evaluation-critic.test.mjs:1851 | GREEN |
| F-4 no uncontrolled loop | tests/evaluation-critic.test.mjs:1877 | GREEN |
| G-4 critic import surface | tests/evaluation-critic.test.mjs:2189 | GREEN |
| G-5 critic browser/DOM/network escape | tests/evaluation-critic.test.mjs:2200 | GREEN |
| G-6 critic mutation + tool-execution | tests/evaluation-critic.test.mjs:2213 | GREEN |
| G-7 stripper anti-vacuity | tests/evaluation-critic.test.mjs:2222 | GREEN |
| G-1..G-4 constraint-inference.js scans (zero-import, capability, mutation, anti-vacuity) | tests/constraint-inference.test.mjs:722/728/737/742 | GREEN |
| M-10..M-17 final 3.16-surface scans | tests/constraint-inference.test.mjs:1720–1804 | GREEN (8/8 CLEAN) |
| 3.15 G-1..G-12 correction.js scans | tests/correction.test.mjs | GREEN (144/144 file) |
| 3.13 G-suite ai.js scans | tests/ai.test.mjs | GREEN (131/131 file) |

### H-3 — src/core integrity: **ZERO MODIFICATIONS**

- `git status --porcelain src/core/` → **0 entries**.
- `git diff HEAD --stat -- src/core/` → **empty**.
- 178-file freeze manifest re-verified by the 3.15 probe re-run
  (`3.16-H-core-probe.txt`): aggregate sha256 =
  `137327739471ff095325854c296a82aa84afde0258b396ad6902b22de3e21e56`
  (buffer==utf8 agreement) — **byte-identical to the value accepted at
  3.15-G and carried through 3.15-H**. 17 top-level entries.

### H-4 — Dependency audit: **ZERO npm runtime dependencies**

- `package.json` declares **no `dependencies` and no `devDependencies`
  fields** (grep exit 1); only `name`, `version`, `type`, `scripts`.
- `npm ls --depth=0` → `└── (empty)`.
- package.json sha256 `b329cc0d410cef4f…` = the 3.15-accepted value
  (unchanged through 3.16).
- Counted guard: correction.test.mjs G-10 (whole-src-js relative-import sweep
  + package.json dependency fields) GREEN; the 3.16 file's M-10/M-11 import
  sweeps GREEN.

### H-5 — Network / filesystem scan: **CLEAN**

Counted proof (comment/string-stripped bodies, word-boundary aware):
- **M-10** (fetch / XMLHttpRequest / WebSocket / raw http(s) URLs over the
  stripped bodies of all four 3.16 production files + the test file's import
  discipline: relative specifiers + exactly `node:fs`, `node:crypto` declared
  builtins) — GREEN.
- **M-11** (fs / require / node: in production stripped bodies; all production
  imports relative) — GREEN.
- **M-12** (LLM API vocabulary: openai, anthropic, llm, completion(s),
  embedding, api_key, apikey; no AI-vendor SDK import) — GREEN.

Independent raw corroboration (this checkpoint): case-insensitive grep for
network/fs vocabulary over the four production files returned only false
positives (`refs.` matching `fs.`; no true matches).

### H-6 — History linearity: **PRESERVED**

- `git diff HEAD -- src-js/transaction.js` → empty; sha256
  `5ec1369b16c1294101e19d45bece4250be5fdcb1d1878eb07d6de87e5c0ea5e4` =
  the 3.15-accepted value, also **pinned inside counted test M-16**
  (constraint-inference.test.mjs:1716,1793).
- Linear push-truncation implementation untouched: `HistoryManager.push`
  truncates `transactions` to `currentIndex+1` before appending
  (src-js/transaction.js:163); no DAG, no branch structure.
- Counted faces: M-16 (no HistoryManager reference in the 3.16 stripped
  bodies, no DAG vocabulary) and L-11 (history stays linear through a
  constraint-correction loop: one linear array, currentIndex at tail) and
  M-9 (post-undo push TRUNCATES — the invariant-13 signature) — all GREEN.
- correction.js byte-identical to the 3.15 G-pristine
  (`9e31da8dff4be9b5…`, pinned in M-16/M-17 at constraint-inference.test.mjs:1794/1803).

### H-7 — Transaction boundary: **PRESERVED**

- Import contracts unchanged: ai.js imports exactly `./tools.js` (ai.js:55);
  evaluation.js exactly `./geometry.js` + `./bbox.js` (evaluation.js:48-49);
  critic.js exactly `./evaluation.js` (critic.js:104); constraint-inference.js
  **zero imports**.
- **M-13**: no store-mutation call form, no store/SceneGraph constructor
  vocabulary in any 3.16 production body — the only writer is the transaction
  pipeline. GREEN.
- **M-14**: no SceneGraph node-creation/reparenting/removal vocabulary. GREEN.
- **M-15**: no TransactionBuilder/TransactionExecutor/transactionManager/
  ToolRegistry reference; no `.execute(|.commit(|.undo(|.redo(` call form in
  the 3.16 arms. GREEN.
- **M-17**: no `begin(` call in the production surface — no nested
  transactions. GREEN.
- One-attempt-one-transaction: engine-owned (frozen correction.js:2492
  `executeCorrectionAttempt`; content-derived `atx-` id at correction.js:2530),
  proven live in L-7 (history grows by exactly one per attempt) and in the
  golden M-1 ⑨ (the `atx-` correction transaction is independent of the root
  and drift transactions and sits in the linear history).
- Golden face: M-8 — every store mutation in the golden run is inside a
  committed transaction. GREEN.

### H-8 — Stub scan: **NO STUBS**

- TODO/FIXME/XXX/placeholder/not-implemented/stub/hack marker grep over the
  four production files: **zero true hits**. The only "stub" matches in ai.js
  (:860, :895, :1114, :1163) are pre-existing 3.13 checkpoint-anchor COMMENTS
  (`[CHECKPOINT-C-DISPATCH] … Stub-kill anchor` etc.) — all located BEFORE the
  3.16 hunks, which begin at ai.js:1360 (verified against the diff hunk
  headers `@@ -485,8` and `@@ -1348,3 +1356,194`).
- constraint-inference.js: the only `return null` forms (:129, :137) are the
  not-found paths of the local `findFunctionPath` recursive search —
  legitimate control flow, not placeholders. No `return true` / `return []`
  placeholder forms. Every exported function is fully implemented (module
  re-read in full this checkpoint).
- ai.js S-A `verifyArrangementAgainstConstraints` (ai.js:1457-1512) and S-B
  `verifyPlanStepsAgainstConstraints` (ai.js:1514-1549): complete
  implementations (validation, quantity extraction, tolerance comparison,
  axis classification, conflict aggregation, deep-freeze) — no stubs.
- evaluation.js constraint arm: `evaluate` :980 + internal
  `constraintCompliance` :1104 — complete (tolerance guard, per-constraint
  kernel, deviation emission, metadata merge). No stubs.
- critic.js constraint pass: `constraintProposalFor` :489 routed at
  `proposeCorrections` :406 — complete (provenance lookup, position-class
  translate deltas, honest silence for size/distance classes, dedup,
  ordering). No stubs.

### H-9 — git diff summary (observed)

```
$ git diff HEAD --numstat
201  2  src-js/ai.js
 64  1  src-js/critic.js
166  1  src-js/evaluation.js

$ git status --porcelain
 M src-js/ai.js
 M src-js/critic.js
 M src-js/evaluation.js
?? scripts/phase3.16-evidence/
?? src-js/constraint-inference.js
?? tests/constraint-inference.test.mjs
```

See Discrepancy D-1: the directive's expected figures (ai +103/-0,
evaluation +197/-0, critic +197/-0) do not match the tree; the observed
values above govern. The four deleted lines are the three 3.16 seam
modifications, each disclosed and counted: ai.js:488-492
(`validateExpectedState` accepts the compliance-request form beside the bare
boolean), evaluation.js:964 (`unevaluatedExpectations` flags only the bare
boolean marker), critic.js:416 (`constraintProposalFor(d,…) || proposalFor(d,…)`
routing). Everything else is purely additive.

### H-10 — Pristine snapshot: **CAPTURED**

Captured AFTER GREEN (re-captured this checkpoint at 2026-09-24T06:07:34Z —
see `3.16-H-snapshot.txt`; hash-identical to the earlier same-day capture,
proving no drift between the prior H attempt and this re-verification). Contents:

| File | sha256 | Lines | Exports |
|---|---|---|---|
| src-js/constraint-inference.js (new) | `741f92c43aa0fe7d…` | 377 | 10 |
| src-js/ai.js (M) | `9fb2eece9514f196…` | 1549 | 20 |
| src-js/evaluation.js (M) | `3b9061f071ab8b43…` | 1174 | 12 |
| src-js/critic.js (M) | `305ab38c33081524…` | 509 | 6 |
| tests/constraint-inference.test.mjs (new) | `6a733b5c95c6b2d2…` | 1809 | — (106 tests) |
| src-js/correction.js (frozen ref) | `9e31da8dff4be9b5…` | 3335 | — |
| src-js/transaction.js (frozen ref) | `5ec1369b16c12941…` | — | — |
| package.json (unchanged) | `b329cc0d410cef4f…` | — | — |
| scripts/run-suite.sh (unchanged) | `34d21656bbc3059e…` | — | — |

Export identification:
- constraint-inference.js (10): `T19_SUPPORTED_TYPES`, `T19_HOUSE_MAPPING`,
  `T19_PROPOSAL_KEYS`, `InferenceErrorCodes`, `InferenceError`,
  `validateInferenceRequest` (:186), `inferConstraints` (:220),
  `validateInferredConstraint` (:268), `toHouseConstraintRecord` (:316),
  `validateHouseConstraintRecord` (:336).
- ai.js (20; the 3.16 additions are `EXPECTED_STATE_CONSTRAINT_QUANTITIES`
  :1393, `verifyArrangementAgainstConstraints` :1457,
  `verifyPlanStepsAgainstConstraints` :1514 — the 17 pre-3.16 exports
  unchanged).
- evaluation.js (12; the A/B-era surface pinned — the 3.16 compliance arm is
  INTERNAL, `constraintCompliance` :1104, no new exports).
- critic.js (6; the C/D-era surface pinned — the constraint rule is INTERNAL,
  `constraintProposalFor` :489, no new exports).

---

## PART II — FINAL BUILD REPORT (22 SECTIONS, spec §97)

### 1. STATUS
**COMPLETE — 1049 GREEN (943 counted + 5 gates; 106 inference).** All 12
acceptance gates PASS (with two disclosed evidence limitations, see
Discrepancies D-2/D-3 — neither blocks). All 33 reconstructed
Definition-of-Done items satisfied. No STOP condition triggered. Ready for
the approver's COMMIT + PUSH decision; nothing committed or pushed.

### 2. BASELINE
Regression chain verified from the phases' own H evidence: 3.13 = 679
(131 ai + 548 prior), 3.14 = 799 (+120 evaluation-critic file), 3.15 = 943
(+144 correction file), 3.16 = 943 + 106 (constraint-inference file, run
separately per the H-1 protocol) = **1049**. This checkpoint re-ran the full
tree: 943/943 + 5/5 gates exit 0; 106/106 exit 0; typecheck exit 0
(`3.16-H-full-suite-final.txt`, `3.16-H-inference-suite-final.txt`,
`3.16-H-typecheck-final.txt`). Invariant 20 (baseline holds) preserved
exactly — no pre-existing test was modified or removed
(`git status` = 3 modified hosts + 2 new files + evidence dir only).

### 3. IMPLEMENTATION
The 3.16 surface = 1 new module + 3 additive host arms + 1 test file:
- **src-js/constraint-inference.js** (NEW, 377 lines, 10 exports, ZERO
  imports): T19 constraint inference — fixed phrase table (:68-92),
  deterministic UUID-v4-format content ids (:163-180), request validator
  (:186), inference entry (:220), proposal validator (:268), house-record
  projection (:316), pass-through record validator (:336). Deep-frozen plain
  data everywhere; no entropy (four seeded FNV-1a passes, no Date.now/Math.random).
- **src-js/ai.js** (+201/−2): `validateExpectedState` now accepts the
  compliance-request form `{records:[…], tolerance?}` beside the 3.14 bare
  boolean (ai.js:488-496); two pure planning arms —
  `EXPECTED_STATE_CONSTRAINT_QUANTITIES` (:1393), `PLANNER_PINNED_AXES`
  (:1406), `PLANNER_TOOL_AXES` (:1412), `verifyArrangementAgainstConstraints`
  (:1457, the DATA face), `verifyPlanStepsAgainstConstraints` (:1514, the
  STATIC MUTATION face). No scene access; import contract unchanged.
- **src-js/evaluation.js** (+166/−1): the constraint-compliance arm INSIDE
  `evaluate` (:980; compliance request consumed :1009-1022; metadata
  extension :1027-1030) with the internal kernel `constraintCompliance`
  (:1104). NO new exports (12-export A/B-era surface pinned); property
  namespacing — position.x/y, size.width/height, position.distance.
- **src-js/critic.js** (+64/−1): the constraint rule INSIDE
  `proposeCorrections` (:406; routing `constraintProposalFor(d,…)
  || proposalFor(d,…)` at :416; rule at :489). NO new exports (6-export
  surface pinned). Position-class violations → §25 transform/translate
  proposals (delta = expected − actual, pinned axis only); size/distance
  classes honestly silent (§28: the only size capability T06 is
  origin-anchored — no SAFE capability; surfaced, never patched).
- **tests/constraint-inference.test.mjs** (NEW, 1809 lines, 106 tests,
  Categories A–M): data contracts, vocabulary mapping, pass-through,
  provenance, no-mutation, determinism, architecture scans, backward
  compatibility, planner/evaluation/critic/loop integration, golden
  scenario, properties P-1..P-7, final scans.

### 4. T19 CONTRACT
Vocabulary = the A-2/D-8 intersection exactly: `{equalWidth, equalHeight,
align, center}` — frozen (`T19_SUPPORTED_TYPES`, constraint-inference.js:42;
counted A-1 :350). House mapping complete: align → the four edges; center →
both center axes (`T19_HOUSE_MAPPING` :44-49; counted A-2, B-3, B-4, B-5).
T19 NEVER infers hard constraints — strength vocabulary `{strong, weak}`
(:62, :208-210, :296-298). Proposal key set exactly the 8 T19 keys (:51-53;
counted A-5). Ids are content-derived UUID-v4-FORMAT, stable across
paraphrases and runs (counted A-6, F-3, M-4/P-2). Honesty: unmapped
utterances yield zero invented proposals, reported in `unmatched` (:262;
counted B-6).

### 5. CONSTRAINT MODEL
Accepted records live in the LIVE ConstraintStore (constraints.js:318,
create :320, list :336). Pass-through discipline: the proposal id survives
acceptance unchanged (`toHouseConstraintRecord` :316-331 preserves
id/objectIds/strength; counted C-1..C-4 — all eleven live house types
validate, store round-trip byte-identical). Provenance rides in the record:
`{t19Type, inferredBy:'T19'}` (:329; counted D-3). Strength model: T19
infers strong/weak; hardening to `required` is an explicit user act (the
golden fixture does exactly that, M-1 ③). Post-acceptance drift model:
accepted constraints OUTLIVE their originating plan — the compliance request
`{records, tolerance}` in `ExpectedState.constraint.satisfied` declares the
accepted regime as the desired state (ai.js:488-496; evaluation.js:1009-1022;
test header :177-198).

### 6. PLANNER
Two rule-based, conservative, scene-free verifications (§43 sanction):
- DATA face `verifyArrangementAgainstConstraints` (ai.js:1457-1512):
  per-enabled-constraint pinned-quantity extraction against the reference
  (objectIds[0]); verdicts SATISFIED / VIOLATED (with violated participant)
  / UNVERIFIABLE (OBJECT_MISSING / QUANTITY_ABSENT / UNSUPPORTED_TYPE) /
  DISABLED; aggregate PRESERVED | UNVERIFIABLE | VIOLATED; deep-frozen;
  deterministic (counted I-1..I-5, I-8).
- STATIC MUTATION face `verifyPlanStepsAgainstConstraints` (ai.js:1514-1549):
  per-step mutated-axis classification (T05 by delta, T08/T09 by axis param,
  T07/T12 none, T06/T10/T11/unknown conservatively everything, :1412-1442);
  HARD (`required`) constraints only; conflict requires touching participants
  AND pinned-axis intersection; unclassifiable constraint types skipped,
  never guessed (:1532) (counted I-6, I-7).
- Backward compatibility: the 3.13 planning contracts unchanged (counted
  H-3); import contract exactly `./tools.js` (ai.js:55; pinned by the 3.13
  G-suite, still GREEN).

### 7. EVALUATION
The compliance arm rides INSIDE the §55 `evaluate` entry
(evaluation.js:980): a plain-object `constraint.satisfied` with a `records`
array triggers `constraintCompliance` (:1104) against the observed world
boxes; a bare boolean stays the 3.14 unevaluated marker (:964). Deviations
join the §13 deviation list; provenance in `metadata.constraintDeviations`
+ `metadata.constraintResults` (:1027-1030). Kernel discipline: tolerance
boundary inclusive-satisfied / beyond-deviation (counted J-11); first
failing participant in objectIds order localized (J-6); missing participant
/ unmeasurable geometry → UNVERIFIABLE, never invented (J-8, J-9); disabled
invisible (J-10). §12 record assembled by the engine's own
`createEvaluationResult` (:536) — validates through the LIVE validator
(J-13); deterministic byte-identical (J-14, M-3); read-only (J-12).
Deviation classes: alignLeft drift → exactly one §13 deviation
category=geometry, property=position.x, expected/actual/delta, targetRef
`$doc:` (counted J-2); size classes report size.width/size.height (J-3, J-4);
center axes (J-5); fixedDistance (J-7). The 12-export surface and the
`./geometry.js`+`./bbox.js` import contract unchanged (B-30, G-1 GREEN).

### 8. CRITIC
The constraint rule lives inside the §23 rule engine (`proposeCorrections`
critic.js:406) with NO new exports and the `./evaluation.js` import contract
unchanged (:104; C-3, G-4..G-7 GREEN). Routing: a deviation is
constraint-owned iff `metadata.constraintDeviations` carries its deviationId
(:489ff) — otherwise the 3.14 rules run verbatim (counted H-2, K-10).
Position-class (position.x/position.y) → one 7-key §25 proposal
`{type:'transform', targets:[violated], operation:'translate', params:{x,y}}`
with delta = expected − actual on the pinned axis, 0 elsewhere (counted K-2,
K-3; validated as a LIVE §25 record and PLANNABLE through createPlan → T05
in K-12/K-14). Size-class (size.width/height) and distance-class → NO
proposal — §28 honesty: T06's origin-anchored scale moves the object; no
SAFE capability; surfaced in K-4/K-5, never bypassed (counted, disclosed in
the test header :45-54). Deterministic, ordering = deviation order (priority
= index), dedup on canonical intent+targetRef+objectId (K-6, K-7, K-8).
Entry contract guards: non-EvaluationResult → CriticError
INVALID_EVALUATION_RESULT (K-9); orphan constraintDeviations skipped (K-11);
read-only over the record, frozen proposals (K-13).

### 9. CORRECTION LOOP
The FROZEN 3.15 CorrectionEngine (correction.js:3329; sha-pinned) closes
constraint agendas end-to-end with ZERO engine changes: L-1 (TERMINATED /
VERIFIED, §3 choreography), L-2 (plan carries hardConstraintsPreserved /
softTradeOffs), L-3 + M-7 (HARD violated pinned axis → plan refusal
HARD_CONSTRAINT_REJECTED → CONSTRAINT_BLOCKED, zero attempts — the REJECTED
face; the golden's non-conflicting hard equalHeight is PRESERVED byte-identical
through the loop — the PRESERVED face), L-4/L-5 (honest UNFIXABLE /
NO_CAPABILITY boundaries — the frozen derivation table is rule-less for T06
and zero-recipe for CONSTRAINT — never patched), L-6 (no-constraint-context
requests still close with NO false safety claim), L-8 (§22 call accounting:
the host critic is called exactly 1 + iterations times, document verbatim —
the loop never self-evaluates), L-9 (regression → §15 declaration → §32 undo
restores the drifted state; constraint records untouched by rollback), L-10
(determinism of outcomes), L-12 (a lying critic is answered NO_PROGRESS —
VERIFIED requires the authority), L-13 (two-axis sequencing), L-14 (audit
trail: attempt.transactionId + §11 expectedImprovement), M-1 (the golden,
below). Safety gates unchanged and live: `evaluateCorrectionSafety`
(correction.js:1870), plan generation (:1956), attempt execution (:2492)
with the content-derived `atx-` transaction id (:2530).

### 10. HISTORY
Linear throughout (Invariant 13): HistoryManager untouched
(transaction.js:163 push-truncation; sha 5ec1369b… = 3.15-accepted; pinned
in-test M-16). No DAG vocabulary in any 3.16 body (M-16); L-11 proves one
linear array with currentIndex at the tail through a constraint loop; M-9
proves the post-undo push truncates (the invariant-13 signature). The
correction attempt is ONE independent substrate transaction per attempt
(L-7); history accounting in the golden: creations + drift + correction =
historyAfterCreation + 2, currentIndex at tail (M-1 ⑪). Session bookkeeping
pushes no history (L-14). No nested-transaction API anywhere (M-17; the
builder cannot nest — transaction.js unchanged).

### 11. TESTS
106 counted tests in `tests/constraint-inference.test.mjs` (Categories:
A 9 data contracts, B 8 vocabulary mapping, C 4 pass-through, D 3
provenance/confidence, E 5 no-mutation, F 3 determinism, G 4
architecture scans, H 3 backward compatibility, I 8 planner integration,
J 14 evaluation integration, K 14 critic integration, L 14 correction-loop
integration, M 17 golden + properties + final scans) — **106/106 GREEN**.
943-test counted suite + 5 gates — **GREEN, exit 0**. Typecheck — exit 0.
Harness protocol identical to the 3.15 house runner (final
`Tests: N total…` line, exit 1 on failure; constraint-inference.test.mjs:99-120).
RED/GREEN history: `3.16-red.txt` (the whole-file RED — the suite authored
before the module existed: ERR_MODULE_NOT_FOUND), `3.16-green-file.txt`
(106/106).

### 12. PROPERTY TESTS (spec §66 — each a real executable test)
- **P-1 Determinism** — M-3 (:1595): identical substrate state →
  byte-identical evaluation JSON, inference, and critique (Invariant 17).
- **P-2 Identity** — M-4 (:1609): same logical constraint → same
  content-derived id across paraphrases/runs/acceptance; any logical change
  moves the id.
- **P-3 Scope** — M-5 (:1623): a constraint over [A,B] is blind to C — at
  the compliance arm AND at the safety gate.
- **P-4 Non-Mutation** — M-6 (:1643): the canonical store snapshot is
  byte-identical across inference, compliance, and critique.
- **P-5 Hard Constraint** — M-7 (:1656): accepted hard constraints never
  silently degraded — gate REJECTS conflicting fixes (L-3 face), PRESERVES
  non-conflicting hard records (golden face), store records survive
  byte-identical.
- **P-6 Transaction** — M-8 (:1672): every store mutation in the golden is
  inside a committed transaction; the linear history covers
  creations + drift + correction; the arms hold no write path.
- **P-7 History** — M-9 (:1686): constraint corrections never produce
  non-linear history — undo restores without appending; post-undo push
  TRUNCATES (the invariant-13 signature).
All seven GREEN in the final run.

### 13. GOLDEN TEST (spec §67 — M-1, :1534)
The deterministic END-TO-END loop over the LIVE substrate — three-rectangle
column (x=100, w=80, h=60 at y=100/200/300; fixture :122-175), creation
through the REAL pipeline (Intent → createExpectedState :872 → createPlan
:889 → validatePlan :1205 → compilePlanToDSL :1348 → parseDSL → validateDSL
→ compileToIR → DSLExecutor.execute → Tool Registry → TransactionExecutor
:192 → Commit). Eleven evidence items, all asserted live:
① committed column state (:1537-1543); ② T19 proposal set
[inferConstraints :220] = equalWidth, equalHeight, alignLeft in table order
(:1544-1546); ③ accepted records in the LIVE ConstraintStore
[toHouseConstraintRecord :316 → ConstraintStore.create :320] — equalHeight
hardened to required by the user act (:1547-1550); ④ Planner outputs:
ExpectedState 'requested' + validating plans with the documented T01→T07
slice (:1551-1556); ⑤ the REAL post-acceptance drift transaction
[TransactionBuilder.begin/addCommand/build :142 + execute :192] id
`tx-drift-g3`, moving rect 3 to x=130 (:1557-1560); ⑥ EvaluationResult
status DEVIATION — exactly one §13 deviation (position.x 100→130, delta 30)
+ metadata.constraintDeviations naming the accepted record id
(:1561-1566); ⑦ exactly one §25 proposal — translate x=−30 on the drifted
object (:1567-1570); ⑧ loop TERMINATED / VERIFIED, one attempt
(:1571-1574); ⑨ the independent content-derived `atx-` correction
transaction, in the linear history, distinct from root and drift
(:1575-1578); ⑩ re-evaluation PASS — zero deviations, zero constraint
violations (:1579-1582); ⑪ final verdict — left edge restored to 100,
linear history = creations + drift + correction (:1583-1585).
Determinism face M-2: two full golden runs → byte-identical canonical
traces (uuid/digest identity-entropy projection disclosed :1483-1492).

### 14. ARCHITECTURE
The 3.16 module: ZERO imports, ZERO capability words, ZERO mutation forms
in the stripped body (counted G-1..G-4). The hosts: import contracts pinned
and unchanged; the arms add NO export to evaluation.js/critic.js and reach
no store/scene/transaction/execution surface (M-13..M-17). Final scans
M-10..M-17 cover the whole 3.16 surface (the four production files + the
test file's import discipline) for fetch/HTTP/network, fs, LLM-API
vocabulary, direct Store writes, SceneGraph mutation, transaction bypass,
History DAG, nested transactions — **8/8 CLEAN**. Frozen sha pins carried
in-test: transaction.js `5ec1369b…`, correction.js `9e31da8d…`
(constraint-inference.test.mjs:1716-1717). src/core 178-file aggregate
unchanged. Scan-style discipline: new G-pattern counted tests appended
(NOT spliced into frozen A-era blocks) — the A-era raw scans remain in
place byte-identical (tests/evaluation-critic.test.mjs unmodified).

### 15. DEPENDENCIES
Zero npm runtime dependencies. No `dependencies`/`devDependencies` fields
(package.json); `npm ls --depth=0` empty; package.json sha unchanged
(`b329cc0d…`). All imports across the 3.16 surface are relative src-js
modules; the only node: builtins are in the TEST harness (node:fs,
node:crypto — declared, counted in M-10/M-11). Typecheck runs via
`npx -y -p typescript` (a dev-time tool invocation, not a dependency
field — unchanged since 3.13).

### 16. SECURITY
No network capability (M-10); no filesystem access in production surface
(M-11); no LLM/API vendor surface (M-12); no dynamic code evaluation
(eval/Function banned in every scan since 3.13/3.14, re-GREEN); no
capability-word hits in any stripped production body; the arms are pure
functions over plain data (deep-frozen outputs; E-1..E-5 no-mutation
discipline; I-5 frozen verdicts). Prompt-injection surface (the T19
utterance) is data-only: phrase matching over a fixed table, no code paths
influenced by content beyond the mapped vocabulary; unmatched input is
reported, never executed.

### 17. SRC/CORE
Frozen: `git status --porcelain src/core/` = 0 entries;
`git diff HEAD -- src/core/` = empty; 178-file aggregate sha256
`137327739471ff095325854c296a82aa84afde0258b396ad6902b22de3e21e56`
(probe re-run PROBE PASS — `3.16-H-core-probe.txt`). No 3.16 file imports
anything from src/core (the whole-src-js relative-import sweep + zero-import
pins hold).

### 18. PHASE G
No backlog item was absorbed, resolved, or required as a blocker by 3.16
(see PART V — 20/20 NOT TOUCHED, item-level carrier evidence). The 3.16
work added NO new backlog items of its own beyond the boundaries it
inherited and re-surfaced as counted evidence (T06 size-class unfixability
K-4/L-4; CONSTRAINT-category zero recipes L-5) — all disclosed, all
pre-existing boundaries, none patched.

### 19. GIT
Branch `feat/phase-3.15-correction-loop` @ `cd5494d` (0 commits beyond
HEAD). Modified: ai.js, critic.js, evaluation.js. New/untracked:
src-js/constraint-inference.js, tests/constraint-inference.test.mjs,
scripts/phase3.16-evidence/. Nothing staged, nothing committed, nothing
pushed. numstat: `201 2 ai.js / 64 1 critic.js / 166 1 evaluation.js`.

### 20. ACCEPTANCE GATES
All 12 gates PASS — the full table with per-gate evidence is PART III below.

### 21. PREREQUISITE / BLOCKER REPORT
No unresolved prerequisite. No STOP condition triggered. Two evidence
limitations are disclosed (not blockers): (a) the verbatim 3.16 spec text
(§96's 33-item DoD list, §82's 20-invariant enumeration, §87's backlog
itemization) is not re-readable this session — the spec was received as
pasted chat content in a prior session and is not on disk; checklists were
reconstructed from the H-1 directive + the tree's counted evidence
(D-2/D-3). (b) The per-checkpoint A–F RED/GREEN runs of this phase were
delivered in-session (checkpoint STOP reports) and are not separately
persisted as evidence files; only the file-level RED and the final GREEN
are in the evidence directory — the FINAL state is fully re-verified here,
so this is documentation archaeology, not a verification gap.

### 22. FINAL VERDICT
**PHASE 3.16 IS COMPLETE AND VERIFIED.** The constraint-inference pipeline
(T19 → accepted records → constraint-aware planning → constraint-compliance
evaluation → constraint-rule critique → correction loop → independent
correction transaction → re-evaluation → VERIFIED) is proven end-to-end by
106 counted tests over the LIVE substrate with zero changes to the frozen
correction engine, transaction machinery, and src/core tree. 1049 GREEN.
Zero npm dependencies. No stubs. No boundary violations. Pristine snapshot
captured. **Awaiting the approver's decision to COMMIT + PUSH.**

---

## PART III — ACCEPTANCE GATES (spec §76–§87): 12/12 PASS

| Gate | Name | Verdict | Evidence (observed this checkpoint) |
|---|---|---|---|
| §76 A | Data Model | **PASS** | A-1..A-9 + C-1..C-4 GREEN: exact T19 proposal key set (:51-53), house-record shape validates for all 11 live types, pass-through identity preserved, LIVE store round-trip byte-identical; §12/§25 records validate through the LIVE validators (J-13, K-12, g3Critic guard). |
| §77 B | T19 | **PASS** | B-1..B-8 + D-1..D-3 GREEN: the four-type intersection, edge/center semantics, unqualified-centered expansion, honest unmatched, content-derived ids, provenance {phrase, rule:T19:*}, confidence in (0,1]. |
| §78 C | Planner | **PASS** | I-1..I-8 GREEN + H-3 backward compatibility: DATA face (I-1..I-5), STATIC MUTATION face (I-6..I-7), self-consistency (I-8 — the planner's own ExpectedStates satisfy the T19-inferred regime), scene-free, import contract unchanged. |
| §79 D | Evaluation | **PASS** | J-1..J-14 GREEN: the arm inside evaluate() (:980), §13 deviations with namespaced properties, metadata constraintDeviations/Results, tolerance boundary, UNVERIFIABLE honesty, determinism, read-only. No new exports. |
| §80 E | Critic | **PASS** | K-1..K-14 GREEN: constraint rule inside proposeCorrections (:406/:489), 7-key §25 translate proposals, §28 honest silence for size/distance, dedup/ordering/determinism, entry guards, PLANNABLE output (K-14). No new exports. |
| §81 F | Correction Loop | **PASS** | L-1..L-14 GREEN over the FROZEN engine (sha-pinned): VERIFIED closure, preserved/trade-off records, HARD_REJECTED face, UNFIXABLE/NO_CAPABILITY honesty, §22 call accounting, rollback arc, determinism, audit trail. |
| §82 G | Architecture (20 invariants) | **PASS*** | All counted scans GREEN: G-1..G-4 (new module), M-10..M-17 (whole surface), 3.13/3.14/3.15 G-suites still GREEN. Named invariants verified: Inv 8 (only-transaction mutation: M-8, M-13, M-15), Inv 13 (linear history: M-9, L-11, M-16, transaction.js:163), Inv 17 (determinism: F-1..F-3, J-14, K-6, L-10, M-2, M-3), Inv 20 (943 baseline: H-1). *The verbatim 20-item enumeration is not re-readable this session (spec not on disk) — verification covers the four named invariants + the full counted scan surface; disclosed as D-2, not a failure. |
| §83 H | Dependencies (zero npm) | **PASS** | No dependency fields in package.json; npm ls empty; hash unchanged; M-10/M-11 import sweeps; correction G-10 sweep GREEN. |
| §84 I | Frozen Tree (src/core) | **PASS** | 0 porcelain entries; empty diff; 178-file aggregate `13732773…` = accepted value (probe PROBE PASS). |
| §85 J | Security / Boundary | **PASS** | M-10 (no network) / M-11 (no fs) / M-12 (no LLM API) CLEAN; no eval/Function; raw corroboration grep clean; prompt-injection surface is data-only phrase matching. |
| §86 K | Regression (943 + N) | **PASS** | 943/943 + 5/5 gates exit 0; 943 = exactly the 3.15 baseline; +106 = 1049; no pre-existing test modified (git status). |
| §87 L | No PHASE G Scope Creep | **PASS** | Diff = 3 hosts (additive constraint arms) + 2 new files + evidence dir; zero backlog carriers touched (PART V); no unrelated feature, cleanup, or refactor anywhere in the diff. |

## PART IV — DEFINITION OF DONE (spec §96): 33/33 ✓

**Provenance disclosure (D-2/D-3):** the verbatim §96 checklist is not
re-readable this session (the 3.16 spec was pasted as chat content in a
prior session; `upload/` holds only the 3.11–3.15 specs). The 33 items below
are reconstructed from the H-1 directive's own deliverables + the spec
anchors cited throughout the phase (§66/§67/§74/§76–§87/§99). Each item is
verified against observed evidence; none is asserted without a test or a
hash.

| # | Item | Verdict | Evidence |
|---|---|---|---|
| 1 | Counted suite 943 GREEN | ✓ | `3.16-H-full-suite-final.txt`, exit 0 |
| 2 | 5 standalone gates GREEN | ✓ | same file, gate PASS ×5 |
| 3 | Inference suite 106 GREEN | ✓ | `3.16-H-inference-suite-final.txt`, exit 0 |
| 4 | Total 1049 GREEN | ✓ | 943 + 106 |
| 5 | Typecheck clean | ✓ | `3.16-H-typecheck-final.txt`, exit 0 |
| 6 | T19 vocabulary = A-2/D-8 intersection | ✓ | constraint-inference.js:42; A-1 |
| 7 | T19 house mapping complete + frozen | ✓ | :44-49; A-2, B-3..B-5 |
| 8 | Proposal data contract exact | ✓ | :51-53; A-5, A-8 |
| 9 | Content-derived deterministic ids | ✓ | :163-180; A-6, F-3, M-4 |
| 10 | Pass-through acceptance, no re-id | ✓ | :316-331; C-1..C-4 |
| 11 | Provenance + confidence contract | ✓ | D-1..D-3 |
| 12 | Inference no-mutation discipline | ✓ | E-1..E-5 |
| 13 | Inference determinism (byte-identical) | ✓ | F-1..F-3 |
| 14 | Planner DATA face | ✓ | ai.js:1457-1512; I-1..I-5, I-8 |
| 15 | Planner STATIC MUTATION face | ✓ | ai.js:1514-1549; I-6..I-7 |
| 16 | Planner import contract unchanged | ✓ | ai.js:55; 3.13 G-suite GREEN; H-3 |
| 17 | Compliance arm inside evaluate(), no new exports | ✓ | evaluation.js:980/:1104; H-1, J-* |
| 18 | §12 record shape preserved (metadata extension only) | ✓ | J-1, J-13; createEvaluationResult :536 |
| 19 | Constraint rule inside proposeCorrections, no new exports | ✓ | critic.js:406/:489; H-2, K-* |
| 20 | §25 7-key proposal contract preserved | ✓ | K-2, K-12; validateCorrectionProposal :271 |
| 21 | Loop integration through FROZEN engine only | ✓ | correction.js sha-pinned (M-16/M-17); L-1..L-14 |
| 22 | Golden scenario M-1 with 11 evidence items | ✓ | :1534-1586 |
| 23 | Golden determinism M-2 | ✓ | :1588-1593 |
| 24 | P-1 Determinism | ✓ | M-3 |
| 25 | P-2 Identity | ✓ | M-4 |
| 26 | P-3 Scope | ✓ | M-5 |
| 27 | P-4 Non-Mutation | ✓ | M-6 |
| 28 | P-5 Hard Constraint | ✓ | M-7 (+L-3) |
| 29 | P-6 Transaction | ✓ | M-8 |
| 30 | P-7 History | ✓ | M-9 |
| 31 | Final architecture scans 8/8 CLEAN | ✓ | M-10..M-17 (:1720-1804) |
| 32 | src/core frozen (178 files) | ✓ | H-3 evidence |
| 33 | Zero npm dependencies | ✓ | H-4 evidence |

## PART V — PHASE G BACKLOG STATUS (spec §87): 20/20 NOT TOUCHED

The 20 items = the 17 itemizations recorded in the committed 3.15 BUILD
REPORT (`git show cd5494d:scripts/phase3.15-evidence/3.15-H-BUILD-REPORT.md`,
backlog table lines 510-535) + the two 3.15-specific items (worklog Task
F-G1) + the F-44 T08 boundary. Verification method: every carrier is either
byte-identical (git diff empty / sha equal) or, where the carrier file WAS
touched by 3.16 (evaluation.js, ai.js), the diff hunks demonstrably do not
intersect the item's code region (3.16 hunks: evaluation.js :964 + :1009-1030
+ arm :1032-1174; ai.js :488-496 + :1360-1549).

| # | Item (source) | Carrier | 3.16 status |
|---|---|---|---|
| 1 | T13 orphan store-GC policy (3.11/3.12 D-2) | stores.js | NOT TOUCHED (sha 7c034936…, no diff) |
| 2 | T11 substrate ordering under nested groups (D-3) | tools.js | NOT TOUCHED (0dc8124f…, no diff) |
| 3 | T10 createGroup array-parentId quirk (D-4) | tools.js | NOT TOUCHED (same) |
| 4 | createTransformObjectCommand no-op (D-5) | transaction.js | NOT TOUCHED (5ec1369b…, no diff) |
| 5 | T14 real outline w/ font system (D-6) | tools.js | NOT TOUCHED |
| 6 | T17 parametric kernel-bypass revisit (D-7) | dsl.js | NOT TOUCHED (e35b3132…, no diff) |
| 7 | semantic heading→title alias (D-8) | semantic.js | NOT TOUCHED (e00419fa…, no diff) |
| 8 | U1 dsl fallback direct-delete (D-9) | dsl.js | NOT TOUCHED |
| 9 | create-with-opacity residual (3.11/3.12-F) | appearance.js/tools.js | NOT TOUCHED (5694ae53… / 0dc8124f…) |
| 10 | G-1 frozen recovery item | tracked recovery tests | NOT TOUCHED (no test file modified) |
| 11 | interaction.test_1.mjs orphan residual | that file | NOT TOUCHED (absent from git status) |
| 12 | SpatialIndex invalidation on TransactionCommitted (3.13-8) | scenegraph.js/transaction.js | NOT TOUCHED (39fcea94… / 5ec1369b…) |
| 13 | A-era paren scans → G-pattern upgrade (3.13-10) | tests/ai.test.mjs | NOT TOUCHED (file byte-identical; 3.16 added its own G-suite in a NEW file) |
| 14 | A-30 lacks `fs` (3.14-G) | tests/evaluation-critic.test.mjs:547-557 | NOT TOUCHED (file byte-identical) |
| 15 | A-31 lacks `.write(`/lowercase `.set(` (3.14-G) | tests/evaluation-critic.test.mjs:558-568 | NOT TOUCHED |
| 16 | buildActualState bbox dispatch → path/star/polygon (3.14-A disc 1) | evaluation.js | NOT TOUCHED at item level (buildActualState :309-535 outside all 3.16 hunks) |
| 17 | nested-parent transform-delta re-derivation (3.14-C disc 4) | evaluation.js/ai.js | NOT TOUCHED at item level (transform-evaluation + planning regions outside all 3.16 hunks) |
| 18 | Live evaluator bare `opacity` vs house `appearance.opacity` (3.15-E) | evaluation.js | NOT TOUCHED at item level (appearance metric region outside all 3.16 hunks); the 3.16 constraint arm does not evaluate opacity |
| 19 | T11 recipe object-id vs group-node-id (3.15-E) | correction.js | NOT TOUCHED (9e31da8d… = G-pristine) |
| 20 | Engine-driven T08 route boundary (F-44) | correction.js | NOT TOUCHED (same); 3.16 proposes T05 translates only |

No backlog item was RESOLVED, none was required as a blocker, no unrelated
cleanup performed — compliant with "No unrelated backlog cleanup is
permitted."

---

## PART VI — DISCREPANCIES vs SPEC / DIRECTIVE

- **D-1 — Directive's diff-size expectations are stale (documentation
  discrepancy, not a code defect).** The H-1 prompt states ai.js +103/−0,
  evaluation.js +197/−0, critic.js +197/−0 and constraint-inference.js
  "335 lines". Observed on the final tree: ai.js **+201/−2**,
  evaluation.js **+166/−1**, critic.js **+64/−1**; constraint-inference.js
  **377 lines**. The observed values are the ground truth reported
  throughout this report (H-9/H-10). The three deleted lines are the three
  disclosed 3.16 seam modifications (ai.js:488-492, evaluation.js:964,
  critic.js:416) — each covered by counted tests (I-*, J-1, H-2). The
  prompt's own code-state claims (1049 GREEN, 106 tests, golden VERIFIED,
  7 properties, 8 scans CLEAN) all verify TRUE; only the size figures are
  stale — most plausibly captured from an interim state before the
  A–G checkpoints concluded.
- **D-2 — The verbatim 3.16 spec text is not re-readable this session.**
  The 3.16 spec was received as pasted chat content (prior sessions);
  `upload/` holds only the 3.11–3.15 specs. Consequently the §96 33-item
  DoD list (PART IV) and the §82 20-invariant enumeration (Gate G) are
  faithful RECONSTRUCTIONS from the H-1 directive + counted evidence, not
  verbatim walk-downs. Every reconstructed item carries independent
  evidence; the reconstruction risk is that a spec-only phrasing nuance is
  missed, not that any behavior is unverified.
- **D-3 — §87 backlog itemization reconstructed from the committed 3.15
  report.** The 20 items (PART V) = 17 committed itemizations + 2 3.15
  items + F-44. The H-1 directive's "20 backlog items" matches this union
  exactly; the match is reported as observed arithmetic, not a spec quote.
- **D-4 — The 106-test file is NOT registered in `scripts/run-suite.sh`.**
  Deliberate, disclosed in the test file header (:5-9): run-suite.sh keeps
  its 3.15 file list; the H-1 protocol itself specifies the two separate
  runs (943 counted + 106 inference = 1049). Prior phases registered their
  file at acceptance (3.15: +1/−1). The approver should decide whether 3.16
  follows the registration precedent at COMMIT time or keeps the
  separate-run protocol; the tree currently satisfies the directive as
  written (run-suite.sh sha `34d21656…` unchanged, exactly as the H-9
  expectation implies by omission).
- **D-5 — Minor line-ref drift in the directive.** B-29 cited as
  evaluation-critic.test.mjs:880 → actual :878; B-30 cited :888 → actual
  :883. All other directive line refs (A-2 :198) verified exact.
- **D-6 — Contract-gap intersection (per the directive's request).** The
  contract gaps surfaced in 3.15 and by 3.16's own testing, checked for 3.16
  intersection: (1) live bare `opacity` vs `appearance.opacity` — no
  intersection (the compliance arm evaluates constraints, not appearance);
  (2) T11 recipe group-node-id — no intersection; (3) T06 rule-less
  derivation (size-class UNFIXABLE) — DIRECT intersection, re-surfaced as
  counted evidence K-4/K-5/L-4, honestly unpatched; (4) T08 engine-driven
  route (F-44) — no intersection (3.16 proposes T05 only); (5)
  CONSTRAINT-category zero recipes — intersected as counted evidence L-5,
  unpatched (pre-existing frozen table); (6) no SAFE size capability (§28,
  the critic face of gap 3) — K-4. All six remain open boundaries; none was
  patched, none narrowed.

## PART VII — UNVERIFIED ITEMS

1. **Spec-verbatim checklists** (D-2/D-3): the §96 / §82-enumeration /
   §87-itemization texts cannot be quoted against this session's records;
   reconstructed as disclosed.
2. **Post-push CI behavior** cannot be observed from this environment (no
   GitHub credentials; push is the approver's act). Prior phases' CI ran
   green; the counted suite is identical in mechanism.
3. **Per-checkpoint A–F evidence files of this phase** are not persisted in
   the evidence directory (only the file-level RED and the final GREEN
   runs). The FINAL state is fully re-verified here (1049 GREEN, all
   hashes, all scans); the gap is historical documentation, not
   verification.
4. **`npm ls` availability**: the command ran and reported empty; it is
   advisory evidence only — the normative proof of zero dependencies is
   the package.json field absence (counted).

---

## STOP REPORT

=== STOP: CHECKPOINT H, PROMPT H-1 COMPLETE ===
Full suite: 943 + 106 = 1049 GREEN
Acceptance Gates: 12/12 PASS
Definition of Done: 33/33 ✓
PHASE G items touched: 0 (20/20 NOT TOUCHED)
Pristine snapshot: CAPTURED (scripts/phase3.16-evidence/3.16-H-snapshot.txt)
Final Build Report: DELIVERED (scripts/phase3.16-evidence/3.16-H-BUILD-REPORT.md)
Awaiting approval for COMMIT + PUSH

NO commit made. NO push made. Snapshot only.
