# PHASE 3.16 — CONSTRAINT INFERENCE INTEGRATION
# FINAL BUILD REPORT (H re-run, r2)

Generated: 2026-09-24T18:28Z (r2) | Spec: origin/docs/spec-3.16 @ dfce612 (verbatim, fetched this session) | Report template: spec §97 | Discipline: English-only, observed values only, no fabrication (§100 rule 7).

## 1. STATUS

**PARTIAL** — by the §96 completeness rule. Every functional gate and scan PASSES (12/12 gates, 1049 GREEN, all scans CLEAN, src/core frozen, zero deps). Exactly ONE §96 item is unsatisfied: item 31 "Documentation complete" (no `src-js/constraint-inference.README.md`; details §21). The phase becomes COMPLETE when that item is resolved by (a) an approved module README per §93, or (b) a documented approver waiver of the module-README convention.

## 2. BASELINE

- Commit: `cd5494d89cb519404a963cbab9faae87d08d076b` (`git rev-parse HEAD`), branch `feat/phase-3.15-correction-loop`, 0 commits beyond the 3.15-closure commit.
- Node: v24.21.0.
- Tests before: 943 counted + 5 standalone gates (3.15-H baseline; re-proven unchanged this run, `3.16-H-r2-full-suite.txt`).

## 3. IMPLEMENTATION

- Files added: `src-js/constraint-inference.js` (377 lines, 10 exports); `tests/constraint-inference.test.mjs` (1809 lines, 106 tests); `scripts/phase3.16-evidence/` (evidence artifacts, fresh r2 names for this run).
- Files modified: `src-js/ai.js` +201/−2; `src-js/critic.js` +64/−1; `src-js/evaluation.js` +166/−1 (`git diff HEAD --numstat`, fresh).
- Files deleted: none.
- Diff shape: 4 total deleted lines, each a disclosed 3.16 integration seam (ai.js :489-490 boolean-validation widened; critic.js :416 rule dispatch; evaluation.js :964 unevaluated-marker narrowing); everything else is pure addition, every hunk marked PHASE 3.16.

## 4. T19 CONTRACT

- Actual input: `validateInferenceRequest` (constraint-inference.js:186) over a plain request carrying `objects` (id-bearing) and `utterance`; strength defaults to `'strong'` (:227). Verified by tests B-block (:449) RED→GREEN.
- Actual output: frozen proposals with keys exactly `T19_PROPOSAL_KEYS` = id / t19Type / houseType / objectIds / strength / source / confidence / provenance (:51-53); ids are content-derived and deterministic (:163-180 class; F-1..F-3 GREEN).
- Proposal behavior: T19 vocabulary = `T19_SUPPORTED_TYPES` = {equalWidth, equalHeight, align, center} (:42) = the A-2/D-8 intersection (test A-1 :350); house mapping frozen (:44-49); canonical house records pass through unchanged (C-1..C-4); unsupported input declines honestly, never invents a verdict (E-block, L-5 face).

## 5. CONSTRAINT MODEL

- Types: 11 house types handled — alignLeft/Right/Top/Bottom, alignCenterX/Y, horizontal, vertical, equalWidth, equalHeight, fixedDistance (`CONSTRAINT_PINNED_PROPERTIES`, evaluation.js arm; `EXPECTED_STATE_CONSTRAINT_QUANTITIES`, ai.js:1393).
- Provenance: deterministic `{phrase, rule = 'T19:<t19Type>'}` (D-1 :569, GREEN); correction-side provenance rides `metadata.constraintDeviations` by deviationId (critic.js:483-486).
- Confidence: numeric, deterministic (D-2/D-3 GREEN).
- Authority: explicit `strength` field; `'required'` = hard, else soft; hard drives the static conflict check (ai.js:1514 block skips non-required) and the correction gate (L-3).
- Hard/Soft: hard constraints never silently degraded (M-7 + L-3 GREEN); soft trade-offs declared on the loop plan (`softTradeoffs`, L-2).

## 6. PLANNER

- Integration: accepted records consumed two ways — the ExpectedState compliance request (`{records, tolerance?}`, ai.js:489-496) and two pure planning-time verifiers `verifyArrangementAgainstConstraints` (:1457) / `verifyPlanStepsAgainstConstraints` (:1514); read-only, no scene access, import contract unchanged (exactly `./tools.js`; 3.13 G-suite GREEN this run).
- ExpectedState: `constraint.satisfied` accepts boolean (3.14 marker) or compliance request; otherwise INVALID_PARAMETER (counted tests, I-block :822).
- Determinism: frozen verdicts, stable ordering (I-block + M-3 P-1 GREEN).

## 7. EVALUATION

- Constraint metrics: per-constraint pinned-quantity comparison against the reference `objectIds[0]`; ONE violation per constraint (first failing participant); UNVERIFIABLE on missing/unmeasurable participants — nothing invented.
- ConstraintDeviation: category `geometry`, property namespaced `position.x|position.y|size.width|size.height|position.distance`; per-constraint verdicts in `metadata.constraintResults`; provenance in `metadata.constraintDeviations` (evaluation.js arm :1042-1174).
- Tolerance: explicit; default `EVALUATION_TOLERANCES.geometry` = 1e-9 (B-29 :878 GREEN); non-finite/negative tolerance throws (counted, J-block).
- Regression: violation detection across the accepted regime post-drift (J-block; L-9 rollback arc; M-7).

## 8. CRITIC

- Inputs: EvaluationResult deviations + `metadata.constraintDeviations` provenance; constraint provenance owns its deviation EXCLUSIVELY (K-10/K-11 guards against guessing and orphan records).
- Outputs: §25 7-key CorrectionProposals, transform/translate (T05), delta = expected − actual on the pinned axis, 0 elsewhere; position-class only (K-2/K-3); size/distance classes decline per §28 (K-4/K-5 — honest, counted).
- Mutation boundary: read-only plain data, frozen proposals (K-13); no tool/transaction execution surface (F-3 :1851, G-6 :2213 GREEN); no new exports (6-export surface pinned, grep-verified).

## 9. CORRECTION LOOP

- Integration: constraint agendas execute through the FROZEN 3.15 CorrectionEngine (L-1 :1268 GREEN; correction.js byte-identical to 3.15-G pristine).
- Independent transactions: ONE attempt = ONE independent substrate transaction with content-derived `atx-` id, distinct from root and drift transactions (L-7 :1352 GREEN).
- Rollback: the rollback arc regresses the relation, declares the regression (§15), undo restores, constraint records untouched (L-9 :1370 GREEN).
- Convergence: NO_PROGRESS on non-improving critics; TERMINATED/VERIFIED only on real re-evaluation (L-12 :1410; L-1 one-attempt closure).

## 10. HISTORY

- Linear invariant: single linear array, currentIndex at tail; post-undo push TRUNCATES (`HistoryManager.push`, transaction.js:163, read verbatim this run); L-11 + M-9 GREEN.
- History changes: NONE — transaction.js sha256 `5ec1369b16c1294101e19d45bece4250be5fdcb1d1878eb07d6de87e5c0ea5e4` = the 3.15-H recorded value (3.15-H-final-hashes.txt:18).

## 11. TESTS

- New tests: 106 (`tests/constraint-inference.test.mjs`: A9 B8 C4 D3 E5 F3 G4 H3 I8 J14 K14 L14 + M17).
- Full tests: 943 counted (17 files) + 5 standalone gates.
- Pass: 943 + 106 = 1049. Fail: 0. Skipped: 0.
- Evidence: `3.16-H-r2-inference-suite.txt` (exit 0), `3.16-H-r2-full-suite.txt` (exit 0, `COUNTED SUITE: 943 passed / 0 failed (of 943)`), `3.16-H-r2-typecheck.txt` (exit 0).

## 12. PROPERTY TESTS

- Determinism: M-3 (:1595) whole-chain byte-identical. Identity: M-4 (:1609) stable content-derived ids. Scope: M-5 (:1623) reads only referenced objects. Non-mutation: M-6 (:1643) canonical snapshot byte-identical across the chain. Hard constraint: M-7 (:1656). Transaction boundary: M-8 (:1672). History: M-9 (:1686). All GREEN this run.

## 13. GOLDEN TEST

- Scenario: M-1 (:1534) — document → T19 → accepted records → planner pipeline → drift transaction → evaluation → critic → correction loop → correction transaction → re-evaluation → VERIFIED, eleven evidence items live.
- Result: GREEN; two full runs byte-identical canonical traces (M-2 :1588).

## 14. ARCHITECTURE

- Invariants 1-20 (§82): PRESERVED. Faces: 1/2/5/11/12 (pre-existing, carriers byte-identical); 6/7 (planner read-only — I-block, G-suite); 8 (M-8/M-13/M-15); 9 (DSL untouched, dsl.js e35b3132); 10 (renderer untouched, G-3 :737); 13 (L-11/M-9); 14/15 (ExpectedState compliance request is not canonical state — J/H blocks); 16 (L-8 — critic called 1+iterations times, verbatim request); 17 (M-3/F-block); 18/19 (M-10/M-11/M-12 + raw greps, zero hits); 20 (H-block backward compatibility :754 + full suite GREEN).
- Violations: none. Scan evidence: `3.16-H-r2-scan-carriers.txt` (395 per-test checks, 0 fail) + 3.16 G-1..G-4/M-10..M-17.

## 15. DEPENDENCIES

- npm runtime dependencies added: **0**. package.json declares no dependency fields; `npm ls --depth=0` = (empty), exit 0.

## 16. SECURITY

- LLM: none (M-12 + raw grep over 811-line surface: 0 hits). Network: none (M-10 + raw grep: 0). Filesystem: none in the production surface (M-11 + raw grep: 0; the test harness's `node:fs`/`node:crypto` imports at test :65-66 are the declared scanner exception). Direct Store mutation: none (M-13/M-14/M-15 + raw call-form grep: 1 benign hit = `Set.prototype.add` on a function-local dedup Set, constraint-inference.js:229-232).

## 17. SRC/CORE

- Modified files: **none**. `git status src/core/` clean; `git diff HEAD -- src/core/` empty; 178 tracked files; aggregate sha256 (git-order) `b555d9ba1de7f96893e2102f2d6ce17d762c1ff4a91e748729a39314af360957` (`3.16-H-r2-core-manifest.txt`); porcelain entries 0; HEAD unchanged since the 3.15-H verification ⇒ bit-identical frozen tree.

## 18. PHASE G

- Items encountered (3.16-specific faces, all surfaced as counted evidence, none patched): (1) T06 origin-anchored scale = no SAFE size capability (K-4/K-5); (2) size-class INSUFFICIENT_EVIDENCE refusal (L-4); (3) CONSTRAINT-category frozen zero-recipe table (L-5); (4) hard-constraint axis-conflict refusal face (L-3).
- Items changed: 0.
- Items remaining: 20/20 open — see the H-14 backlog table in the checkpoint report (NOT TOUCHED, item-level, carriers hash-verified).

## 19. GIT

- Branch: `feat/phase-3.15-correction-loop`. Commit: HEAD `cd5494d` — nothing committed this phase. Working tree: 3 modified + 3 untracked entries (numstat in §3). Push: NOT PERFORMED (§98 discipline).

## 20. ACCEPTANCE GATES

- Passed: 12/12 (§76 A through §87 L; item-by-item table in the checkpoint report, H-12). Failed: 0.

## 21. PREREQUISITE / BLOCKER REPORT

- Any blocker: ONE — §96 item 31 "Documentation complete" (governing §93). Evidence: `ls src-js/constraint-inference.README.md` → "No such file or directory"; the 3.14/3.15 H checkpoints each delivered module READMEs (evaluation/critic/correction.README.md present); no docs/ directory exists; the only 3.16 documentation on disk is in-code section headers + the test-file header. NOT silently patched (§101); reported for approver decision.
- No architectural blockers; §89 STOP never triggered — T19 → Planner → Evaluation → Critic → Correction fully proven (M-1 golden).
- Provenance disclosures (carried from the reconcile decision, all non-blocking): (a) diff-shape delta vs the C–F checkpoint-reported figures is unresolved (no per-checkpoint 3.16 evidence files exist on disk); the current tree was functionally validated and ACCEPTED by the approver. (b) The A–G worklog entries were absent; restored this session in summary form from current-tree evidence. (c) The A–F per-checkpoint evidence files were never persisted; this run generates fresh r2 H-evidence only.

## 22. FINAL VERDICT

**PARTIAL** — per §96 ("COMPLETE only when all are true"): 32/33 DoD items verified with fresh observed evidence; item 31 (Documentation) OPEN pending the approver's README-or-waiver decision. All 12 acceptance gates PASS; 1049/1049 tests GREEN; frozen tree, dependency, security, history, transaction, and scope-creep disciplines all verified CLEAN.
